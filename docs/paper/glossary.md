# Glossary of Key Terms

*   $\mathfrak C_{\mathrm{Borch}}$: finite Borchers/reflection certificate for wedge-reflection, CPT, and spin-statistics use.
*   $\mathfrak C_{\mathrm{sig}}$: predictive well-posedness signature certificate selecting a one-time Lorentzian branch in covered second-order sectors.
*   $\mathfrak W_{D_4}$: $D_4$ continuum-witness audit containing shell moments, curvature-transfer, noncollapse, and recovery-limit data.
*   $\mathfrak C_{\mathrm{Cas}}$: homogeneous Casimir spectral certificate for pre-registered electroweak or gauge-sector finite-part computations.
*   $\mathfrak GY_U^{(4)}$: relative four-dimensional Gelfand-Yaglom determinant certificate for branch prefactors.
*   $\mathfrak C_{\mathrm{meter}}$: metered actualization certificate for a retained interface register and its $\ln2$ timing subledger.

*   **PU Mathematical Response Completeness:** (Definition P.14.9g) Nonempty model class, total normalized registered response maps on every model, and one equivalence class under every registered finite-protocol response equivalence. One constructed model proves only nonemptiness; completeness also requires the universal comparison of all models.
*   **Finite Four-Protocol Response-Table Witness:** (Proposition P.14.9i.1) Exact two-bit finite response table exhibiting normalized read, step, meter, and diagonal laws, a strict two-candidate minimizer, and an explicit nomination/response mismatch. With the registered definition $PP=1/(1+PE)$, its Brier calculation gives $PE=3/8$, $PE_0=1/2$, $\Delta Q=1/8$, and $PP=8/11$. It is not a full PU model or a physical-realization certificate.
*   **Global-Lift Completeness:** (Definition P.14.9g) A selected compatible sector tuple has exactly one ambient lift modulo the declared global response equivalence.
*   **Empirical Realization Identification:** (Definition P.14.9g) A forward-locked calibrated comparison between the unique mathematical response class and observed physical records; it is empirical and is not used to define the selected class or cost.
*   **Realization Non-Entailment:** (Theorem P.14.9h) A satisfiable formal theory cannot entail the truth value of a fresh actuality predicate absent from its axioms, even if its registered mathematical response quotient is a singleton.

*   **Bianchi Law--Source Equivalence:** (Theorem I.4a) At fixed coarse-graining scale, a spacetime-dependent effective coupling plus correction tensor can be rewritten exactly as a constant coupling plus a conserved effective source; only protocols that separately identify the factors distinguish the descriptions.
*   **Flavor Complement Defect:** (Theorem T.79.9) Let $V_u,V_d:\mathbb C^3\to\mathcal H_{\mathrm{int}}$ be isometries and set $C=V_u^\dagger V_d$ and $P_u=V_uV_u^\dagger$. Then $\mathbf1-C^\dagger C=V_d^\dagger(\mathbf1-P_u)V_d\succeq0$ measures the component of the down-type carrier outside the up-type carrier. It vanishes exactly when the two carrier ranges coincide, equivalently when $C$ is unitary.
*   **Minimal Purification Gauge:** (Theorem G.1.11d) The free $U(r)$ fiber of rank-$r$ amplitudes $w$ with $ww^\dagger=\rho$; it is a response redundancy for system-only protocols, not an independent particle or spacetime gauge group.
*   **Nine-Horizon Tensor Tomography:** (Corollary 12.1g.a) A minimal explicit set of nine null contractions reconstructing a trace-free symmetric four-tensor, supplemented by a tenth-direction exact sum rule.
*   **Prediction Codimension:** (Theorem 13.0m) The number $m-r$ of locally independent empirical relations supplied by an $m$-observable prediction map whose Jacobian has rank $r$.
*   **PCE Metric-Response Cost:** (Theorem 12.1e.a) The least Hessian-weighted microscopic cost of a retained metric response, equal to the pseudoinverse quadratic form dual to its emergent covariance.
*   **Shannon Causality:** (Theorem 39c) Exact independence of every pre-lightcone transcript from every independently selectable spacelike context; equivalent to zero mutual information for all priors and zero Shannon capacity.
*   **Static-Holonomy KMS Obstruction:** (Theorem Y.6.1i) Exact stationarity keeps every time-independent retained baryon expectation constant; its value requires a separate symmetry/boundary certificate, and secular generation requires a response-active drive, inflow, preparation, or freeze-out entry.
*   **Vacuum-Shift Response Quotient:** (Definition X.4a; Theorem X.4b) The equivalence class trading constant vacuum stress against the bare cosmological term. Null flux and $\Lambda_{\mathrm{eff}}$ are invariant; normalized modular invariance additionally requires the finite/trace-class identity-response hypothesis.
*   **Whitened Normal Projector:** (Definition 13.0l) The projector $\Pi_\perp=\mathbf1-AA^+$ onto covariance-whitened empirical directions normal to a prediction manifold.

*   **24-Cell:** (Appendix Z, Definition Z.8; Appendix U, Theorem U.30; Proposition U.13a) The standard 24-point configuration on $S^3$ given by the unit Hurwitz integers, realizing the optimal kissing configuration $K(4)=24$ in 4 dimensions and spanning $\mathbb{R}^4$. This is the standard coordinate realization conventionally called the 24-cell vertex set; Theorem U.30 verifies its spherical 5-design exactness. Equivalent, after normalization and orthogonal identification, to the minimal vectors of the $D_4$ root lattice. The sampled angular operator of Proposition U.13a has exact spectrum $0,3^4,8^9,15^8,24^2$; Proposition U.13a.1c identifies these values with the continuum $S^3$ angular eigenvalue values while keeping the $n=3,4$ multiplicity aliasing explicit.

*   **Conditional $A_2$ Absolute-Scale Projection:** (Appendix T, Corollary T.24.16a) On the internally normalized branch,
    $$
    \Sigma_\nu=30.519194\,\mathrm{meV},
    \qquad
    m_\beta=4.62340\,\mathrm{meV}.
    $$
    The branch does not match the cited absolute mass-squared splittings and is not an oscillation-data closure.

*   **$A_c$ (Homogeneous Relaxation Amplitude):** (Postulate K.9.2) The asymptotic enhancement amplitude of the background non-vacuum coupling, defined so that $\Upsilon_G\to 1+A_c$ on the de Sitter branch.

*   **Calibrated Action--Entropy Relation:** (Appendix Q, Theorem Q.0.1; Corollary Q.0.1) The continuum action is fixed relative to an independent action-per-nat scale $\kappa_A$. The branch $\kappa_A=\hbar$ is a calibration condition.

*   **Active Inadmissible Self-Information:** (Appendix A.5.6a) Information whose proof or truth-label may exist externally but cannot be admitted by a predictor as active self-targeted evidence at the targeted cycle without destabilizing the prediction condition. The diagonal object $E_{B,t}$ is inaccessible to $B$ at $t$, accessible to an external predictor, and accessible to $B$ after historical record formation. At the proof-access layer, the correct labeled proof exists in $\mathcal F$, but register-coherent active admissibility forces $\operatorname{LITE}^{\mathrm{act}}_B(t,e_{B,t},N)=\bot$ for every $N$.

*   **Adaptation Driving Force ($\Psi(t)$):** (Definition 20, Equation 24) The net incentive for complexity adaptation $C(t)$: marginal power-equivalent performance gain ($\Gamma_0 \partial PP/\partial C$) minus marginal resource cost rate ($\lambda R' + R'_I$).

*   **Adaptive Dark Fluid:** (Corollary K.9.3b) The effective conserved FLRW fluid generated by the time dependence of $\Upsilon_G$, with
$$
\rho_{ad}=(\Upsilon_G-1)\rho_{nv},
\qquad
p_{ad}=(\Upsilon_G-1)p_{nv}-\frac{\dot\Upsilon_G}{3H}\rho_{nv}.
$$

*   **Admissible Bridge-Law Class:** (Definition H.4.2.8a) The class of positive dimensionless bridge coefficients $\eta$ in $g_0=\eta c^2\sqrt{\Lambda/3}$ that satisfy dimensional closure, internal-data closure, factor locality, no observational fitting, and positive response.

*   **Admissible Counterfactual Variation:** (Appendix P.16a.3; Section 14.2.2) A proposed change of a branch output $X_i$ is physically meaningful only if it is realized by a finite-response deformation of the retained branch parents, certificate gates, and accepted overlap maps, with the image remaining in the same admissible branch. If the deformation exits the branch, the expression remains syntactically writable but has no PU physical content on that branch. This criterion separates branch non-admissibility from conspiratorial setting-hidden-variable coordination.

*   **Aggregate Complexity ($C_{agg}$):** (Definition 29) The Predictive Physical Complexity of an MPU aggregate. Must exceed $C_{op}$ for emergent biasing (Theorem 34).
*   **$\alpha,\beta$ (Alpha, Beta):** (Definition 8) The task-, score-, window-, baseline-, and branch-indexed lower and upper boundaries of a registered Space of Becoming, with $0<\alpha<\beta<1$. Theorem 8 defines $\alpha$ from the matched baseline. On a joint Theorem-9/Theorem-19 branch, the exact response-law endpoint satisfies $\alpha<\beta\le\beta_0$, where $\beta_0$ is the excitation ceiling, and finite complexity gives $PP<\beta$. On an alternative excitation-only branch, Theorem 9 implies the strict viability inequality only when the registered endpoint satisfies $\beta_0<\beta$. Theorems 10–11 do not determine a system-independent scalar $\alpha_{SPAP}$ or an ordering between that task-specific quantity and $\beta$. The bound on $\mathrm{CC}(S)$ is independent (Theorem 39).
*   **$\alpha_{CC,max}$ (Alpha-CC-max):** (Theorem 39) The bounded-bias branch ceiling on Consciousness Complexity (CC). The strict gate $\alpha_{CC,max}<0.5$ forbids deterministic binary endpoint forcing unless a separate finite-response certificate excludes endpoint-complete context pairs. Finite-window zero-error FTL decoding is separately excluded on the regular statistical branch (Definition 10.2a) by Theorem 39a.

*   **$\alpha_{\mathrm{sig}}, \beta_{\mathrm{II}}, \alpha_{\mathrm{err}}$:** Statistical type-I significance level, type-II error probability (power $1-\beta_{\mathrm{II}}$), and target decoding error probability used in Sections 10 and 13. These are distinct from the Space of Becoming parameters $\alpha,\beta$ and from the electromagnetic fine-structure constant $\alpha_{\rm em}$.

*   **$\alpha_{SPAP}$ (Alpha-SPAP):** A class- and protocol-relative quantitative self-prediction boundary when a registered certificate defines one. Theorems 10–11 by themselves exclude a uniform exact self-predictor on their stated diagonal classes but do not determine a system-independent scalar. Theorem 14 gives its log-enhanced quadratic verification/update lower bound only on the separately certified quantitative branch, with transfer to $C_P$ only when the chosen complexity notion lower-bounds those operations.

*   **APS-Eta Baryon Transport Certificate:** (Appendix Y, Definition Y.6.1a; Theorem Y.6.1b) Certificate machinery specifying a four-dimensional Euclidean electroweak interpolation manifold $W_{\mathrm{EW}}$ with APS boundary, a chiral SM Dirac operator $D_W$ twisted by the electroweak connection and the pulled-back flavor Berry bundle, producing two normalized index outputs $I_{CP}$ and $I_{tr}$ that determine $\chi_{CP}$ and $\chi_{tr}$ in Theorem Y.6.1 and Proposition Y.9.1. The canonical unit branch is theorem-level when $I_{CP}=I_{tr}=1$ and the determinant-line orientation product equals $+1$.

*   **APS-Kubo Baryon Transport Certificate:** (Appendix Y, Definition Y.6.1c; Theorem Y.6.1d; Corollary Y.6.1e; Definition Y.6.1f; Theorem Y.6.1g; Corollary Y.6.1h) A finite record combining the APS phase, Berry holonomy, Kubo Chern-Simons diffusion, washout, susceptibility, entropy/photon normalization, and quadrature data. The source identity is $\dot\theta_{\mathrm{PU}}=\dot\Theta_\eta+\int_{S^1}\Gamma^*\mathcal F_{\mathrm{Berry}}$; weighting occurs only after insertion into the transport integral. Exact reduction to Theorem Y.9 requires both the whole-output equality (Y.6.1e.1) and $\mathcal R_B^{\mathrm{APSK}}=\{0\}$; otherwise the theorem-level output is the interval (Y.6.1d.1).

*   **AQFT (Algebraic Quantum Field Theory):** (Appendix F) Framework describing emergent continuum QFT from MPU networks via nets of local algebras $\mathfrak{A}(\mathcal{O})$.

*   **Area Law (Horizon Entropy):** (Theorem 49, Appendix E; Corollary E.8.4g; Theorem E.8.4m) Geometric regularity and the density certificate give a boundary-entropy upper bound. Equality $S_{\mathrm{rel}}=k_B\mathcal A/(4L_{P,\mathrm{op}}^2)+o(\mathcal A)$ additionally requires the capacity-achieving, entropy-saturating, additive-ledger branch of Theorem E.6; $G_{\mathrm{op}}$ is defined by that positive saturated coefficient, and identification with measured $G$ is a separate calibration. The capacity-tight PCE-no-surplus sufficient-min-cut branch satisfying (E.8.4.21) identifies the same saturated entropy with the least sufficient predictive update-current.

*   **Becoming-Flow Compression Datum:** (Appendix X, Definition X.8g.4; Proposition X.8g.5; Remark X.8g.6) Certificate-gated finite record $\mathfrak C_{\Omega}$ packaging a retained state bundle, response metric, reversible operator, dissipative mobility, PCE generator, constraint list, and projection ledger into one branch flow. It compresses accepted reversible, dissipative, KKT, and boundary-variation sector equations when their original branch certificates are already present; it is not an independent law that promotes open gates.

*   **Borchers Reflection Certificate:** (Appendix F, Definition F.10.7d) Certificate $\mathfrak C_{\mathrm{Borch}}$ recording half-sided modular covariance, positivity, reflected extension, Haag-duality overlap, and cone-comparison data so that modular conjugation implements wedge reflection on a covered AQFT wedge family.

*   **Criticality-Tuning Certificate:** (Appendix P, Definition P.8.9a.12.0c; Proposition P.8.9a.12.0d) Certificate $\mathfrak C_{\mathrm{crit}}$ recording a domain state family, predictive-performance observable, control coordinate, susceptibility, critical manifold, distance-to-criticality functional, finite-size-scaling record, and universality-class data. It is required before PU can claim marginal predictability, self-organized criticality, or critical exponents for a domain.

*   **Entropic Clock-Flow Certificate:** (Appendix O, Definition O.3f; Theorem O.3f; Corollary O.3g) Certificate $\mathfrak C_{\mathrm{ECF}}^{(R,q)}$ fixing a retained sector, finite event alphabet, retained entropy-increment estimator, guarantee-level subset when used, null controls, and optional cycle-time calibration. It defines the dimensionless retained clock count
$$
\mathcal T_R=\Sigma_R^{\mathrm{ECF}}/\ln2,
$$
predicts entropic-clock stall when retained entropy/update flow vanishes, and gives the clock-current TUR when the clock is read through a finite Blackwell-PCE record current.

*   **Entropy Retention Ledger $S_{\mathrm{ret}}$:** For a fixed finite retained protocol partition with distribution $p=(p_i)_i$, $S_{\mathrm{ret}}(p)=-k_B\sum_i p_i\ln p_i$ with $0\ln0=0$. This is a state-counting and cost ledger; retained structure additionally requires finite-response relevance and PCE admissibility.

*   **Golay-QEC Bootstrap Record:** (Appendix A, Definition A.0.1q.1; Theorem A.0.2a; Appendix Z, Proposition Z.13b.7) Finite record $\mathfrak C_{\mathrm{GQEC}}$ using the predictive-recovery Golay branch and punctured CSS $[[23,1,7]]$ witness to discharge the QEC compatibility certificate only when physical syndrome, recovery, threshold, and protected-gate ledgers are also accepted.

*   **Predictive Complementarity Allocation Ledger:** (Section 4.5.2, Definition 14.1d; Proposition 14.1e) Certificate $\mathfrak C_{\mathrm{SW}}$ splitting a finite predictor's horizon budget into self-model, world-model, and shared costs. It yields the SPAP-near tradeoff $C_{\mathrm{world}}\le B_C-C_{\mathrm{sh}}-c_{\mathrm{SPAP}}\log(1/\delta_{\mathrm{self}})/\delta_{\mathrm{self}}^2$ for covered tasks.

*   **Predictive Free-Energy Envelope Certificate:** (Appendix D, Definition D.6.5c; Proposition D.6.5d) Certificate $\mathfrak C_{\mathrm{Pred2}}$ under which $F_{\mathrm{pred}}=-V$ and a recorded viable running envelope provide the branch-level “second law of prediction” reading. It applies to the certified expectation or envelope, not to every lineage.

*   **Predictive Generalized Second Law Certificate:** (Section 12.5.4, Definition 12.5.3l; Theorem 12.5.3m) Finite retained horizon-cut record $\mathfrak C_{\mathrm{PGSL}}$ under which generalized entropy is monotone by relative-entropy data processing. It supplies first-difference monotonicity; null convexity and Einstein-branch saturation require the separate $\mathfrak C_{\mathrm{NC}}$ record.

*   **Predictive Update-Current Entropy:** (Appendix E, Definition E.8.4k; Theorem E.8.4l) The conditional-mutual-information current $\mathcal S_{\mathrm{upd}}(\Gamma;\gamma)=\sum_j I(M_j;Y_{\Gamma,j}\mid\mathcal F_j)$ through a retained predictive cut along a finite update path. It is nonnegative, nonincreasing under deterministic post-processing, and bounded by the corresponding channel capacities. At one slice, $I(M_0;Y_{\Gamma,0}\mid\mathcal F_0)=H(M_0\mid\mathcal F_0)$ exactly when $H(M_0\mid Y_{\Gamma,0},\mathcal F_0)=0$; stationarity is not required.

*   **Predictive-Equivalence Ledger:** (Appendix M, Definition M.6.6a; Proposition M.6.6b) Certificate $\mathfrak C_{\mathrm{PEq}}$ recording when perspective-relative actuality, temporal access, action/entropy, finite-frontier, and curvature/source readings are projections of the same retained predictive-update cost ledger through already accepted unit bridges.

*   **Reflexive-Charge Ledger:** (Appendix Q, Definition Q.0.7g.1; Proposition Q.0.7g.2) Finite loop-sector record $\mathfrak C_{\mathrm{ref}}$ with integer charge $q_{\mathrm{ref}}=k$ satisfying $N\ln2+\Delta=2\pi k$ on closed retained predictive loops. The charge is invariant under continuous deformations inside the recorded loop domain.

*   **Assumption 1 (Context-Dependence):** (Section 9.1.2) ND-RID transition probabilities depend on local MPU aggregate state. Basis for CC hypothesis.

*   **Axiom 1 (POP):** (Section 2.1) Prediction Optimization Problem: adaptive systems maximize expected predictive quality $\Delta Q$ under resource constraints ($C_P$, energy, time).

*   **Axiom 2 (Predictive Capacity):** (Section 2.2) Effective prediction ($\Delta Q > 0$) requires an internal model capturing regularities using Information (Definition 1).

*   **Axiom 3 (Operational Viability):** (Section 3.3.6) Sustained operation requires Predictive Performance $PP(t) \in (\alpha, \beta)$.

*   **$B_3$ (Three Bits):** (Theorem 15) The least visited-context log-capacity on the realization class satisfying (O1)–(O3) and (FC): $N_{\mathrm{vis}}^{\min}=8$ and $K_0\equiv B_3=3$. It is not a universal lower bound for self-reference or above-chance prediction.

*   **Benford Mantissa Law (PU conditional form):** (Theorem P.8.9a.12.5) Uniform logarithmic mantissas, or convergence to uniform mantissas under non-lattice multiplicative phase mixing, give first-digit probabilities $\mathbb P(D=d)=\log_b(1+1/d)$.

*   **Biological Fractal Scaling:** (Corollary P.8.9a.12.8) A self-similar biological branching package with copy number $n$ and length ratio $r$ gives effective dimension $D_f=\log n/\log(1/r)$ over the scale range where the branching assumptions hold.

*   **Black Hole Information Problem:** (Appendix K.3, Theorem K.3, Corollary K.3a, Theorem K.3c, Theorem K.3e, Corollary K.3f) In PU, information is preserved globally but local recovery is constrained by perspectival channels, reflexive extraction cost, horizon edge-mode capacity, and predictive island Markov-blanket minimization. Page-curve consistency follows on the approximate-unitary-$k$-design branch, while horizon edge modes implement predictive anomaly inflow. The finite-budget predictive uncertainty gate of Theorem K.3e bounds joint configuration-momentum trapping of hidden remnants, and Corollary K.3f gives positive leakage probability when the normalized support exponents $\delta_X=\log_\Lambda|X|$ and $\delta_P=\log_\Lambda|P|$ satisfy $\delta_X+\delta_P<1$.

*   **Blackwell-PCE Classicality:** (Appendix M.6.11) Classical records arise as PCE-minimal sufficient statistics of finite measurement channels. The selected output algebra is commutative because it is a quotient record algebra, not because a separate classical ontology is added.

*   **Optional Certificate-Gated Extension Record:** A forward-locked finite branch record whose acceptance conditions are exactly those in its local definition. A quantitative record fixes every type, unit or normalization map, residual or tail interval, overlap and forward-lock entry, and falsifier required by its comparison before validation. Benchmark source rules, phenomenological shapes, external comparisons, and open-problem protocols retain their declared status; acceptance affects only the branch explicitly covered. Primary records: Definitions 12.5.3o.1, O.7.2.3c, O.7.2.5a, K.3d.1a, U.16b, U.27a, U.48c, Z.13b.P0, Z.9c, N.8.1a, S.6.1a, I.5c, M.6.5a, T.17a.0c, T.17a.0e, X.9.5d.6, U.15i.3, A.6.7, 12.5.5a, 14.1g, 35b, 13.9.12a, and 13.9.12b; Corollary Y.6.1b.3.

*   **Block-Frame Stabilizer Code:** (Corollary G.8.4c.0a.1) The finite-response code whose syndrome is the PCE-selected inactive-sector block decomposition $\mathbb C^3\oplus\mathbb C^2\oplus\mathbb C^1$. Its capacity-saturating determinant-compatible retained stabilizer algebra is $\mathfrak{su}(3)\oplus\mathfrak{su}(2)\oplus\mathfrak u(1)$.

*   **Bordism-Anomaly Spectrum Closure Gate:** (Theorem G.8.4c.0e) The selected gauge algebra is not enough to close the matter spectrum. A chiral matter/defect package is selected only when its total finite anomaly-bordism class vanishes after accepted defect inflow and the response quotient carries a strict PCE minimizer.

*   **Bordism-PCE Global Anomaly Gate:** (Appendix X.8d.2–X.8d.4) Global anomaly audit requiring the full bordism-valued anomaly character $\alpha_{\mathsf B}\in\operatorname{Hom}(\Omega_{d+1}^{G_{\mathrm{PU}}},U(1))$ to vanish after allowed boundary/interface inflow for any declared redundancy. It includes torsion anomalies invisible to local anomaly polynomials.

*   **Born Rule:** (Theorem 8.3, Theorem 28a, Theorem G.1.7) On a complex Hilbert branch of dimension at least three, every normalized positive noncontextual weight that is additive on finite effect decompositions has the form $P(i)=\operatorname{tr}(\rho P_i)$. For $\rho=|\psi\rangle\langle\psi|$ and $P_i=|i\rangle\langle i|$, this gives $P(i)=|\langle i|\psi\rangle_s|^2$. PCE/PPI supplies the response quotient on its branch; the Hilbert, normalization, noncontextuality, and additivity premises remain explicit selector hypotheses.

*   **Bures Metric and Bures Distance:** (Appendix Z, Section Z.17) For density operators $\rho$ and $\sigma$, write $f(\rho,\sigma)=\operatorname{Tr}\sqrt{\sqrt\rho\,\sigma\sqrt\rho}$ and $D_B(\rho,\sigma)=\sqrt{2-2f(\rho,\sigma)}$. The Bures metric is the infinitesimal Riemannian metric obtained from the second-order expansion of $D_B^2$; on a regular one-parameter branch its line element is $ds_B^2=\tfrac14F_Q(\theta)d\theta^2$. On pure states this infinitesimal metric agrees with the Fubini-Study metric in the same normalization, while the two global distance functions differ. Appendix Z uses this metric on the branch where the SLD quantum Fisher tensor is the registered interface distinguishability tensor.

*   **$C \equiv C_P$ (Predictive Physical Complexity):** (Section 2.4.1, Equation 1) Hierarchical complexity measure for constructing a microstate $\mu$ relative to a fixed universal machine, reference state, and nested constraint family (notation note: $\mu$ is also used as a spacetime index in Sec. 11; context disambiguates). Its finite-valued domain consists of microstates with a common finite full-constraint witness; the definition and Lemma 1 do not prove that this domain is nonempty. The measure is generally uncomputable.

*   **$C_{op}$ (Operational Threshold):** (Definition 13) The task-, environment-, model-class-, baseline-, and accuracy-indexed infimum of $C_P$ over qualifying adaptive-loop implementations, with the extended-real convention for an empty class. It is attained only under a separate existence hypothesis. Effective Corollary 3 gives $C_{op}\ge K_0$ on the full-context Hilbert realization branch with the declared complexity-capacity bridge.

*   **Cavitation-Boundary Temporal-Wave Certificate:** (Appendix L, Definition L.8.2b; Theorem L.8.2c) Finite record containing the cycle-rate perturbation, coherent-charge dipole transfer, positive line or positive-measure spectral support, Maxwell radiation-zone branch, and nonnegative channel-power ledger. It licenses only the resulting conditional electromagnetic component and derives neither the transfer function nor detector-band overlap from acceleration alone.

*   **$C_{PPC}(PP_{target})$:** (Definition 18) Minimum $C_P$ required to achieve target average predictive performance $PP_{target}$.

*   **$C_s$ (Effective Complexity Scale):** (Definition 19a, Equation 22a) The e-fold complexity scale of the Law of Prediction, $C_s=\hat{C}_{target}/\kappa_{\mathrm{eff}}$. At equilibrium, $C_s=\Gamma_0(\beta-PP^*)/[\lambda R'(C^*)+R_I'(C^*)]$ (Proposition 19a).


*   **Finite Hierarchy Stabilization and Common Realizer:** (Theorem 2.4.1a) A bounded nested hierarchy of integer prefix-program minima stabilizes after finitely many strict increases, and one minimum-length program realizes every refinement simultaneously. The theorem proves existence, not a computable stabilization index.

*   **Score-Order-Invariant Predictive Homeostasis:** (Theorems 21a and 22a; Theorem 6.9a) Sign-corrected target-scale feedback whose local stability is fixed by a Hurwitz determinant and whose robust viability band is fixed by Nagumo inward-pointing inequalities. The construction is invariant under strictly monotone recalibration of the predictive-risk score.

*   **Causal Order-Fraction Record:** (Definition 46e.1; Theorem 46e.1) Comparable-pair statistic for a uniformly sampled flat Alexandrov interval; its $D=4$ mean is exactly $1/10$ before curvature and detector-bias residuals.

*   **Finite Metric-Response Conditioning Certificate:** (Definition 46h; Theorem 46i) A fixed-order-stratum chart, response Jacobian, smallest-singular-value floor, derivative-Lipschitz bound, and metric-chart Lipschitz bound that convert finite response error into a local emergent-metric error bound. No universal rigidity claim survives when the conditioning floor closes.

*   **Complete Passivity:** (Definition G.1.9.3b; Theorem G.1.9.3c) Absence of cyclic work extraction from every tensor power. On the faithful simple-spectrum branch it is equivalent to a Gibbs state of the physical Hamiltonian.

*   **$\mathfrak B_{mass}$ (Conditional Mass Branch):** (Theorem N.5) Intersection of the saturated-boundary, saturated-activity completed-reset, and accepted action--entropy gates. The absolute mass-per-information coefficient is asserted only on this branch; physical heat equality additionally needs an overhead-free Landauer implementation and a physical-equilibrium certificate.

*   **$\mu_0^{alg}$ (Algebraic Mass Normalization):** (Definition Z.8f) The coefficient $m_P/(2\sqrt{8\varepsilon_0})$. It becomes a physical mass-per-information coefficient only on $\mathfrak B_{mass}$ and a physical mass per Leech norm only after the norm--information calibration is registered.

*   **Latency--Capacity--Dissipation Ledger:** (Definition 35a; Theorem 35b) Vector edge datum separating causal delay, reliable throughput, and update entropy cost; the delay-constrained capacity function is their joint Pareto observable.

*   **Temporal Operator-Schmidt Memory Bound:** (Theorem M.6.14c.1) A process-tensor cut of operator-Schmidt rank $R$ requires a complete cross-cut memory of Hilbert dimension at least $\lceil\sqrt R\rceil$.

*   **Temporal Determinant and Rank-Revival Witness:** (Theorem M.6.14c.2) For positive-divisible affine one-time dynamics, the absolute determinant cannot increase and affine rank cannot revive. Either event rejects that divisible one-time explanation, without by itself identifying a unique microscopic memory model.

*   **Affine Selected-Overlap Cycle Audit:** (Theorem D.8.9c.1) Post-selection graph-Hodge test for an already-selected tuple of affine overlap records. Vanishing cycle sums are equivalent to integrability; the cycle-space projection is the irreducible inconsistency residual. This does not replace the acyclic parent-dependency registry.

*   **Pythagorean and Nested Spectral Calibration:** (Theorem V.3.11c.1) Affine KL information projections obey an exact Pythagorean identity, and nested calibration families telescope without double counting. The identity applies only to the registered affine moment families and full-support reference used by the theorem.

*   **Spectral Calibration Condition Number:** (Theorem V.3.11c.2) Inverse covariance floor $1/\gamma$ controlling the sensitivity of calibration multipliers and coefficient moments to registered constraint errors.

*   **Reset-Support Zeno Work Bound:** (Theorem J.4b) Minimum cyclic work needed to reach a registered survival probability when every interrogation closes a nontrivial record and the short-time departure has a positive quadratic lower bound.


*   **$\hat{C}_{target}(t)$:** (Definition 21; Theorem 21a) Internal task-scale coordinate. Equation (38) is sign-corrected homeostatic control; interpreting the coordinate as an external-difficulty estimate requires a separate innovation record.



*   **$\hat{C}_v$ (Operational Complexity Proxy):** (Theorem 1, Definition B.1, Equation B.1) Hermitian operator (identified with quantum circuit complexity) on MPU Hilbert space $\mathcal{H}_v$, with spectrum $\lambda(d)=K_0+\Delta C(d)$. Dynamically aligned with $C_P$ at equilibrium (Theorem 2).

*   **Calibration Holonomy:** (Definition X.9.6i.3; Theorem X.9.6i.4) A sector constant is calibration-internal only when it is a registered holonomy or curvature-residual projection of one finite protocol-atlas calibration connection with fixed normalization before validation comparison. Flat calibration locks constants through the same record; nonzero calibration curvature appears only as an explicit residual class.

*   **Canonical Alpha Comparison-Budget Scales:** (Appendix Z, Remark Z.26d; Corollary Z.26d.1) Arithmetic audit of the canonical Appendix Z comparison-budget scales $c_1u^{*3}$ and $c_1u^{*5}$ with $c_1=\pi/(24\sqrt{K_0})$. At $u^*=2^{1/8}-1$ and $K_0=3$, their sum is $B_{\mathrm{budget}}^{\mathrm{can}}=5.649085604\times10^{-5}$. This number is a budget diagnostic, not a certified residual interval for $R_\alpha$; the candidate hypercharge-recoil (operator realization open) branch uses the degenerate residual bound $R_{\alpha}^{YR\perp}=-0.00009287769839723537\ldots$ from Corollary Z.27.11k.21.1.

*   **Canonical Composition Audit for the Thomson Residual:** (Appendix Z, Definition Z.27.11e.3; Proposition Z.27.11e.4; Corollary Z.27.11e.5; Remark Z.27.11e.6) Finite arithmetic audit of the canonical additive, multiplicative, and exponential completions of the certified interface and curvature entries. The multiplicative cross-response $\Delta_{\times}=-\pi (u^*)^2\operatorname{sinc}(u^*)/288$ accounts for $96.08\%$ of the centered residual and leaves a $26.59$ ppb diagnostic remainder, but the audit is non-closing unless a residual operator or all-orders certificate supplies the missing datum before comparison.

*   **Causal-Order Resource:** (Corollary 14.1c) A finite higher-order process property measured by robustness distance to the convex set of causally separable processes. It may be nonzero without retrocausal signaling and is not a closed-timelike-curve channel.

*   **Causal Structure:** (Definition P.8.1; Definition 35a; Theorems 39c, 46) Intervention-response reachability constrained by certified edge latency and exact pre-lightcone context independence. Classical error correction is not part of the definition; a decoder enters only on a separately registered communication branch.



*   **Causality (Postulate 2):** (Section 10.1.1; Theorem 39c) Exact independence of every pre-lightcone transcript law from every independently selectable spacelike context. Common support and nonzero decoder error are weaker coding properties: they can exclude zero-error decoding while leaving a positive finite-error Shannon channel. Any nonzero pre-lightcone statistical dependence on an independently selectable spacelike context violates Postulate 2, even when the regular common-support, no-loop, and current/precision certificates hold.

*   **CC (Consciousness Complexity):** (Definition 30) A quantitative measure of a system’s emergent biasing capability, defined as the **operational norm** of the linear Probability Modification Map $L_S$: $\mathrm{CC}(S):=\|L_S\|_{\mathrm{op}}$ (Equation 54). It bounds the maximum possible deviation from the Born rule through the Fisher-Rao distance gate of Definition 33 and the probability-shift bounds of Theorem 36. On the bounded-bias branch, its deterministic endpoint strength satisfies $\mathrm{CC}(S)\le\alpha_{\mathrm{CC,max}}<0.5$ (Theorem 39), while Theorem 39a supplies only a finite-window zero-error gate and Theorem 39c requires exact marginal invariance for causal compliance.

*   **CC Evidence Triage:** (Definition 13.0a) The preregistered classification of CC/statistical-influence experiments into support, null, or failure using fixed endpoints, artifact bounds, sign tests, stopping rules, and independent replication.

*   **Certificate-Complete:** A sector status indicating that the strict PPI/PCE certificate has all required finite entries and the overlap maps commute with the previously fixed dependency graph. Theorem D.8.9c selects a unique compatible tuple on an acyclic certificate-complete product branch. Existence and uniqueness of one ambient global realizer require the separate restriction-fiber conditions stated after Theorem P.14.1k.1.

*   **Certificate Remainder:** (Appendix Z, Definition Z.27.11a) A bounded term in a numerical sector whose existence, sign, and allowed interval follow from a specified strict PPI/PCE certificate. It is not an adjustable theoretical tolerance. A sector containing a certificate remainder is theorem-closed only when the remainder is either evaluated exactly or bounded sharply by an accepted residual certificate of the form $\mathfrak O_{\alpha,YR\perp}$ (Definition Z.27.11k.12; Corollary Z.27.11k.21.1), $\mathfrak R_\alpha^{\mathrm{AO}}$ (Definition Z.27.11g), $\mathfrak F_U$ (Definition U.15d), the four-mode record $\mathfrak F_U^{(4)}$ (Definition U.73e), or analogous sector-specific finite records.


*   **Chronometric Curvature-Dephasing Principle:** (Section 11.7.2, Theorem 47c; Appendix S, Theorem S.7.3a) Internal energy splittings act as clocks for proper-time mismatch. The coherent phase-rate invariant is $\mathcal D_{ij}^{\phi}=\hbar|\dot\Theta_{ij}|/|\Delta E_{ij}|=|\Delta\Phi|/c^2$. On the saturated chronometric ND-RID dephasing branch, $\mathcal D_{ij}^{\Gamma}=\hbar\Gamma_{\mathrm{ch}}^{(ij)}/|\Delta E_{ij}|=|\Delta\Phi|/c^2$.

*   **Conditional Reset-Power Viability Inequality:** (Remark Q.0.1c.1) For a registered reset ensemble, $\dot{\mathcal L}_{\mathrm{reset}}\ge\nu\,\overline{H_q(P\mid R)}$. The value $\nu\ln2$ requires a conditionally unbiased binary reset record; the structural value $\varepsilon_0=\ln2$ alone fixes no physical power. (Remark Q.0.1c.1) On the additional conditionally uniform binary-reset branch, a finite MPU aggregate maintaining an internal predictive code persists only if $R_{\mathrm{in}}\ge\varepsilon_0N_{\mathrm{MPU}}\nu_{\mathrm{cycle}}+R_{\mathrm{loss}}$ with rates measured in nats per unit time (or bits per unit time with $\varepsilon_0=1$). This is a necessary Landauer-floor condition for code-maintaining persistence, not a biological classification theorem.

*   **Cogito-to-MPU Bridge:** (Introduction; Appendix P.1–P.3; Theorem P.6.1c.3; Corollary P.6.1c.4; Definition 23) The Cogito certifies an indubitable awareness/process $\mathcal{C}$. Appendix P.3.1 and Thesis P.3.5.1 identify the knowledge-bearing activity with expectation, verification, and update; for every finite self-verifying presentation carrying those data, Theorem P.6.1c.3 constructs a canonical predictive normal form up to natural isomorphism and Corollary P.6.1c.4 identifies prediction as the canonical operational form. A physical MPU representative exists only on a nonempty PPI class carrying finite physicalization, record, maintenance, and update-use data for that loop. If the descended PCE objective attains a no-surplus minimum on that class, Definition 23 names a minimal nontrivial representative an MPU; uniqueness additionally requires the strictness or nondegeneracy conditions of the finite-response quotient theorem. Thus predictive canonicality is theorem-level on the finite self-verifying class, whereas MPU existence and uniqueness are conditional physical conclusions.

*   **Coherence:** (Appendix C) Reliability and stability of predictive correlations and information propagation across the MPU network. Requires geometric regularity (Theorem C.2).

*   **Complexity Hierarchy:** (Section 2.4.1) Nested refinement sequence $\{C_{P,n}\}$ used to define $C_P$ without importing downstream $C_P$-dependent laws. Monotone finite convergence is proved for each microstate carrying one common finite full-constraint witness; existence of such a witness is an independent realizability obligation.

*   **Computational-Thermodynamic Asymmetry Profile:** (Definition O.5.2a; Theorem O.5.2e) Tagged bookkeeping object separating SPAP diagonal obstruction, witness generation, average-case function inversion, protocol-specific erasure cost, and path-measure asymmetry. No cross-coordinate inference is valid without a uniform, distribution-preserving, success-preserving, non-circular bridge theorem.

*   **Compression-Trajectory Schema for an Accepted Registry Snapshot:** (Appendix P, Convention P.14.1k; Convention P.14.1l; Definition P.14.1m; Algorithm P.14.1m.0; Remark P.14.1m.5) Conditional syntactic record-count reading of a nonempty populated accepted finite registry. An admissible step uses a fresh replacement record and composable recovery maps, so the positive integer record count strictly decreases and every maximal trajectory terminates after at most $|\mathcal C^{(0)}|-1$ steps. A listed candidate compressor still requires its own recovery certificate; the list does not populate a snapshot or prove a decrease of information, PCE cost, or physical resources.

*   **Conditional Multifractal Cascade:** (Theorem P.8.9a.12.10) A self-similar PCE-cascade branch with $b\ge2$ branches per step, scale ratio $r>1$, and weights $\{p_i\}$ has exponent function $\tau(q)=\log(\sum_i p_i^q)/\log(1/r)$ when $\tau$ is defined with denominator $\log\varepsilon$. Its singularity spectrum is $f(\alpha)=\inf_q(q\alpha-\tau(q))$, and its large-deviation form is $f(\alpha)=D_{\mathrm{eff}}-I_{\mathrm{unif}}(\mathbf x(\alpha))/\log r$ with $D_{\mathrm{eff}}=\log b/\log r$.

*   **Conditional Universal Pattern:** (Section P.8.9a.12) A universal empirical pattern derived as a theorem only after its domain-specific scale, phase, relaxation, discrete-scale, rank, or biological-network hypothesis is stated explicitly.

*   **Constraint-as-Carrier (PU reading):** (Section P.16) On nonempty prohibition branches, the structural locus to which the cross-cultural vocabulary of *daimon*, *muse*, *the spirit*, *intuition*, *inspiration*, *vocation*, *fate* (in its *character-as-destiny* sense), *karma-as-trajectory-constraint*, *tacit knowledge* (Polanyi 1966), *strange loop* (Hofstadter 1979), and *élan vital* (Bergson 1907) historically refers. PU identifies this locus as the prohibition set $\mathcal{L}^{\neg}_S$, whose causal efficacy is carried by the cost potential $U_S$ (Theorem P.16.1) rather than by any positive degree of freedom. Theorem P.16.2 (Inner-Compulsion Clause) establishes the single-system consequence under the nonempty-branch hypotheses; Proposition P.16.2 (Coupled-Set Cost-Gradient Bias) and Corollary P.16.1 (Bias Form) establish a coupled-system *bias* toward mutually low-cost configurations on the corresponding relevant branches. The strictly stronger reading — that no global coordinating agency whatsoever is required for multi-system coordination (synchronicity, cosmic fate, providence-in-the-large) — additionally presupposes the Environmental SPAP Hypothesis (Hypothesis 14.5.8). Remark P.16.5 gives the term-by-term separation by epistemic status.

*   **Context-State Tomography:** (Appendix P, Definition P.16d.5.1; Theorem P.16d.5.2) Reconstruction of an aggregate context state $\text{context}_S(t)$ from finite interventions and responses. The reconstructable object is the quotient of context space by the registered finite tomography map $\vartheta_B^\Theta\circ\Theta_{S,B}$.

*   **Contextual Holonomy / PCE Magic Cost:** (Appendix G.1.7a) Protocol-cover obstruction measuring whether Born-consistent distributions across incompatible contexts admit a positive global assignment. Its value is the minimum quasiprobability overhead $\log\lVert q\rVert_1$ over PPI-equivalent signed ledgers and vanishes exactly for noncontextual protocol covers.

*   **Continuum Limit / Operational Continuum Limit:** (Section 11, Appendix F) The finite-resolution effective closure by which the discrete MPU network admits a manifold and AQFT compression at accessible scales. It is not the ontological claim that the substrate becomes an actual real-number continuum. On the $M=24$, $D=4$ shell branch, Theorem 43.5 packages the geometric branch using Appendix C.6d–C.6e, Appendix D.6e, Theorem C.6c, and Theorem 44a under their stated curvature-transfer, finite-core, and quantitative-rigidity hypotheses; Appendix F supplies the algebraic net under the controlled coarse-graining hypotheses of Theorem F.0, with lightcone normalization supplied on the strict single-clock ND-RID branch by Corollary F.1a.

*   **Core Discrete Backbone:** The theorem-level minimal finite-response chain $K_0=3$, $N_{\mathrm{vis}}^{\min}=8$, $\varepsilon_0=\ln2$, $d_0=8$, $a=2$, $b=6$, $M=24$, $k=12$, and Euclidean response-carrier dimension $D=4$ on the stated Appendix Z/PPI/PCE branch. Here $N_{\mathrm{vis}}^{\min}=8$ is the finite operational-context result, $d_0=8$ is the minimal complex Hilbert-carrier saturation, and the $3+1$ spacetime interpretation of $D=4$ requires the separate continuum and Lorentzian-promotion certificates. Later numerical sectors may depend on additional certificates or an accepted finite spectral calibration datum without changing this backbone.

*   **Cosmological Acceleration Lock:** (Appendix H, Corollary H.1; Appendix I, Corollary I.3a) On the QFI-gravity bridge representative of Definition H.0 and Equation H.4b,
$$

g_0=\frac{c^2\sqrt{\Lambda}}{8},
\qquad
\Sigma_\dagger=\frac{c^2\sqrt{\Lambda}}{16\pi G}.
$$

*   **Cost Potential ($U_{S,\theta_0}$):** (Definition P.16.3, Certificate-Relative Cost Potential) The extended certified lower-bound field $U_{S,\theta_0}(\theta') := F(\underline\mu_{S,\theta_0}(\theta'))$ on candidate configurations, where $\underline\mu_{S,\theta_0}(\theta') = \inf\{\mu_S(E) : E \in \mathcal{R}_{\theta_0}(\theta')\}$ over the declared class of admissible content-integrating realizers, and $F(u)=0$ for $u<\mu_0$, $F(u)=c\,u^2\log u$ for $\mu_0\le u<\infty$, $F(\infty)=+\infty$, under a uniform Theorem-M.10.3 reduction certificate with constants $c>0$, $\mu_0>1$. Written $U_S$ when $\theta_0$ is clear from context. Whenever $\underline\mu_{S,\theta_0}(\theta')\ge\mu_0$ is finite, every registered realizer satisfies $C_{\mathrm{integrate}}(S,E)\ge U_{S,\theta_0}(\theta')$; a value $+\infty$ from an empty realizer class records unreachability, not SPAP divergence. Certified divergence of $U_{S,\theta_0}$ on $\mathcal{L}^{\neg}_S$ (Lemma P.16.1) provides the rigorous substrate for the cost-stratification language of Theorem P.16.1 and its scale-applicability clauses.

*   **CPTP (Completely Positive Trace-Preserving):** (Appendix E, Appendix G) A quantum channel $\mathcal{E}$ is CPTP if it maps density operators to density operators, preserves trace ($\mathrm{tr}(\mathcal{E}(\rho)) = \mathrm{tr}(\rho)$), and remains positive when extended to any ancilla system. The average 'Evolve' channel $\mathcal E_N$ is CPTP by construction. A registered completed reset gives Proposition E.2a's support-capacity deficit $C(\mathcal E_N)\le\ln d_0-\ln2$ for a binary reset register; strict contractivity ($f_{RID}<1$) holds only on refresh/minorization branches satisfying Lemma E.1. SPAP alone supplies neither channel architecture.

*   **Creative Decompression:** (Appendix P.16b.9–P.16b.10) Shape recognition followed by transport of structural content and hypothesis-level transfer of annotations. Exact structural decompression is sound under typed subdiagram isomorphism and response-presheaf isomorphism (Theorem P.16b.10.3); annotation transfer remains hypothesis generation (Theorem P.16b.10.4); approximate transfer requires explicit discrepancy metrics and stability moduli (Theorem P.16b.10.6).

*   **Cross-Scale Certificate Mining:** (Appendix P, Definition P.16d.18.1; Theorem P.16d.18.2) Certificate discipline requiring independent derivation paths through the PU graph to commute in the finite response quotient before a shared target invariant is accepted relative to a cover.

*   **CTB Model (Context-Targeted Bias):** (Definition 34, Equation 58–59) Example model for CC influence where probability shifts towards a context-defined target distribution: $P_{obs}(i) = (1-\text{CC}) P_{Born}(i) + \text{CC} p_{target}(i)$. In the self-directed case, the same form is applied to an internal physiological event channel of the aggregate, with the neutral channel distribution replacing the external Born baseline and with the shift bounded by $\gamma_a(c)\mathrm{CC}(S)$.

*   **Current-Graph Non-Collapse Decomposition of the Discrete Backbone:** (Appendix R, Proposition R.3.5e.2; Theorem R.3.5e.3; Corollary R.3.5e.4; Remarks R.3.5e.5, R.3.5e.5a, and R.3.5e.6; Appendix Z, Definition Z.35d and Theorem Z.35e) Separation of the recurrent ledger $(K_0,d_0,\varepsilon_0,a,b,M,k,D)=(3,8,\ln2,2,6,24,12,4)$ into source roles $(\mathcal C_3,\mathcal C_{\mathrm{cap}},\mathcal C_{\mathrm{tan}},\mathcal C_{\mathrm{kis}})$ and downstream coherence invariants $(J_{\mathrm{top}},J_{\mathrm{Cl}},J_{\mathrm{ar}})$. The current ledger proves non-collapse of the source roles inside the present proof graph; it does not prove pairwise-disjoint leaf inputs and does not exclude a future common parent invariant. The parent-datum schema gate records the bookkeeping index $(3,2,24,4)$ without promoting it to an autonomous parent invariant.

*   **Curriculum Reconstruction Problem:** (Appendix P, Definition P.16d.13.1; Theorem P.16d.13.2) Finite PCE problem of choosing an instructional sequence that minimizes reconstruction discrepancy, processing cost, and ambiguity overhead for a target response structure.

*   **$d_{\mathcal{N}}$:** (Definition 35) Network propagation cost metric based on minimum cumulative ND-RID cost along paths (shortest path distance in cost space) (Equation 64).

*   **$E_{\mathcal N}$:** (Section 11.2.1) For a fixed finite MPU graph with $2\le|\mathcal V|<\infty$, the dimensionless propagation-efficiency observable is $E_{\mathcal N}=[|\mathcal V|(|\mathcal V|-1)]^{-1}\sum_{u\ne v}\delta/d_{\mathcal N}(u,v)$, with disconnected-pair summands set to zero. It is an average reciprocal propagation-cost distance after edge costs, scale, and branch status are fixed, not the harmonic mean itself and not a law equating distinct physical networks.

*   **Deep Rationality:** (Definition P.16.5, Proposition P.16.1) A trajectory of a Property-R aggregate's self-model that is (i) PCE-optimal on the system's full operational budget (Theorem L.6) and (ii) not reconstructable as PCE-optimal by the deliberative subprocess $\mathcal{D}_S$ (Definition P.16.4) within its verbal-rational standard. Proposition P.16.1 supplies such a transition only when $\mathrm{CC}(S)>0$, the maintained configuration has greater integrated cost than some reachable alternative over the declared window, and both configurations satisfy $PP\in(\alpha,\beta)$. Proposition 15 can make the driving calculation opaque to $\mathcal D_S$ but does not alone establish that a lower-cost reachable alternative exists. Under those hypotheses, the report language listed in Corollary P.16.2 is compatible with the transition without being uniquely implied by it.

*   **$\delta_{SPAP}$ (Delta-SPAP):** (Appendix B.3, Theorem B.2) On a registered task, score, admissible class, and quantitative-boundary certificate, the target margin $\delta_{SPAP}:=\alpha_{SPAP}-\alpha_{\mathrm{tgt}}$ between the preregistered target performance and that class-relative boundary. It appears in the certified lower bound $C_{\mathrm{uni}}=\Omega(\log(1/\delta_{SPAP})/\delta_{SPAP}^2)$ only under the Bernoulli reduction, independence, confidence, and operation-count certificate of Theorem B.2. An observed $PP$ may replace $\alpha_{\mathrm{tgt}}$ only when the same certificate registers that identification and retains its confidence requirement. Transfer to another operational complexity requires its stated domination bridge. The symbol is distinct from the lower Space-of-Becoming parameter $\alpha$.

*   **Description-Arity:** (Appendix P.16b.8) The minimum number of primitive description lenses required to cover the proof requirements of a retained theorem ledger. On the typed non-collapse branch with Primitive-Lens Independence, the semantic ledger requires exactly three lenses: operational/categorical $\mathsf C$, geometric/topological $\mathsf G$, and measure-theoretic/dynamical $\mathsf M$ (Theorem P.16b.8.5).

*   **DHR-Tannaka Response Datum:** (Appendix F, Definition F.5b; Theorem F.5c) Certificate machinery reconstructing the compact response gauge group and field algebra from localized transportable superselection sectors of the PPI-observable AQFT net. Gauge factors with no localized sector are response-null surplus.

*   **Disproportion Gap:** (Definition P.8.9a.11.1) The excess concentration $\Delta_{\rho}(f;x^*)=\rho(f;x^*)-f$, strictly positive for $x^*>0$ and $f\in(0,1)$ by Theorem P.8.9a.11.2.

*   **Dominance of Stabilizing Costs (DSC):** (Theorem 22) The condition that the stabilizing effects of performance saturation (concave $PP$) and the strictly convex physical cost $R(C)$ are sufficient to overcome any destabilizing effects (e.g., from the concave informational cost $R_I(C)$), ensuring the existence of a unique, stable equilibrium complexity $C^*$.

*   **DSRO (Dynamic Self-Reference Operator):** (Definition 11, **Theorem A.1.5**) Formal representation of a computable function whose output depends dynamically on provable properties about itself, based on Kleene's Second Recursion Theorem. Used in SPAP logic.

*   **Dual Dynamics:** (Section 7.3.3) The MPU model separates reset-free Internal Prediction from interaction-triggered `Evolve`. Internal Prediction has a unitary Schrödinger representation only on the continuous time-translation ray-isometry branch of Theorem 8.7. `Evolve` has a Markov-kernel representation on Proposition 28's explicitly stochastic, countably additive, measurable branch with no retained deterministic refinement.

*   **Dynamically Enforced Functional Correspondence:** (Theorem 2, Appendix D) On a true stable PCE equilibrium satisfying DSC and faithful-cost-identifiability/noncompensation, $C_P(v)=\langle\hat C_v\rangle$ for every retained MPU. The work-cost feedback results give tracking to a noise floor under their stated drift and variance hypotheses. Theorem D.5 gives ergodic averaging, and low-noise concentration only on its compact irreducible detailed-balance branch. These are conditional, logically distinct statements.

*   **$E_8$ Root System:** (Appendix R, Section R.2; Theorem R.4.3) The exceptional root system consists of 240 vectors of squared length $2$; its lattice has kissing number $240$, is self-dual, and achieves the optimal sphere-packing density in dimension eight. Appendix R adopts it as an internal-coordinate branch. The classification theorem identifies the carrier with $E_8$ only after an independent positive-definite real, even, unimodular rank-eight lattice carrier is supplied; $d_0=8$ alone does not force that physical realization. On a separately registered root-assignment and hierarchy-law branch, the distinct-root squared distances $d^2\in\{2,4,6,8\}$ constrain the Mass Hierarchy Invariant $\mathcal R$.

*   **EFE (Einstein Field Equations):** (Section 12, Theorem 50; Theorem 12.1a) On the operational Lorentzian continuum branch carrying the local Rindler/KMS certificate, saturated horizon entropy density, conserved Appendix-B source, linearized Raychaudhuri data, and the all-null Clausius relation on shrinking transverse patches with remainder $o(A_\varepsilon h_\varepsilon^2)$, the metric response obeys $R_{\mu\nu}-\tfrac12Rg_{\mu\nu}+\Lambda g_{\mu\nu}=(8\pi G_{\mathrm{op}}/c^4)T_{\mu\nu}^{(MPU)}$ (Equation 76). The action-level uniqueness statement separately requires the four-dimensional metric-only local Lovelock-Wald hypotheses, and identifying $G_{\mathrm{op}}$ with measured $G$ is a calibration.

*   **Einstein Causality (Microcausality):** (Appendix F, Corollary F.1) Standard QFT principle stating that operators associated with space-like separated regions commute: $[\mathfrak{A}(\mathcal{O}_1), \mathfrak{A}(\mathcal{O}_2)] = \{0\}$ if $\mathcal{O}_1$ is space-like to $\mathcal{O}_2$. In the PU manuscript this appears conditionally: it follows from Appendix F once the continuum-bridge hypotheses of Theorem F.0 are satisfied.

*   **Electroweak–Baryon Square-Root Lock:** (Appendix Y, Corollary Y.11.4b; Appendix T, Corollary T.63.2) On the canonical baryogenesis branch,
$$
\eta_B
=
\mathcal C_{\mathrm{eff}}\mathcal F_{CP}f_{\mathrm{wash}}2^{-1/3}
\sqrt{\frac{v}{A_{EW}M_{Pl}}}.
$$

*   **Equivalence–Constitutive Separation Law:** (Appendix N, Theorem N.11a; Section 12, Corollary 12.3.2; Appendix G, Corollary G.4b.2) At one coarse-graining scale and inside one branch and PPI quotient, a retained response channel whose source/response ratio descends to a common ledger is equivalence-principle-bearing on the tested probe class. Metric-universal gravity on the common mass ledger is the canonical sufficient case. The converse holds only on the non-compensation branch with a retained probe class that separates source/response variations: there, equivalence-principle behavior forces each retained variation to be a common normalization, PPI-null, or restricted to a constant-ratio subprobe class. Gauge, material, preparation, and constitutive responses need not satisfy that converse off this branch.

*   **Emergent Metric Fluctuation Theorem:** (Section 12, Definition 12.1d; Theorem 12.1c; Definition 12.1d.4; Theorem 12.1e; Corollary 12.1e.1; Corollary 12.1e.2; Definition 12.1f; Theorem 12.1f.1) Closure of the metric-fluctuation row of the Global Strict-Certificate Ledger on every branch carrying an accepted nondegenerate finite-response gravitational channel ensemble. Linearized emergent metric response and its finite covariance $\Sigma_n$ are deterministic projections of the finite KMS-descent certificate, smooth-envelope record, positive Hessian, reconstruction derivative, and PCE fluctuation scale. No independent microscopic graviton Hilbert sector is added.

*   **$\varepsilon_0$ (Structural Binary Reset-Support Value):** (Proposition 5, Definition 28, Appendix J, Theorem J.1) The log-cardinality of the declared binary reset-support alphabet, $\varepsilon_0=\ln2$. It is not an ensemble-independent lower bound on physical heat.

*   **$\varepsilon_{\mathrm{diss}}$ (Dissipative Overhead):** On a registered cyclic reset, the nonnegative excess in $\varepsilon_{\mathrm{reset}}=H_q(P\mid R)+\varepsilon_{\mathrm{diss}}$. It affects heat and power accounting but is not defined as excess above the structural value $\varepsilon_0$ unless the erased record is separately certified as conditionally uniform binary.

*   **$\varepsilon_{\mathrm{phys}}$ (Registered Reset-Heat Cost):** (Definition 28, Theorem 31, Appendix J) On the cyclic degenerate-register branch, $\varepsilon_{\mathrm{phys}}:=\langle Q_{\mathrm{bath}}\rangle/(k_BT)=H_q(P\mid R)+\varepsilon_{\mathrm{diss}}\ge H_q(P\mid R)$. The total entropy production of the idealized memory-plus-bath cycle is $k_B\varepsilon_{\mathrm{diss}}$; it is not the reset heat itself.

*   **'Evolve' Process:** (Definition 27, Proposition 28, Appendix M) The interaction and update phase of the MPU model. On the explicit stochastic-law branch it is represented by a Markov kernel, and on the complex-Hilbert Born-selector branch its outcome weights have trace form. A reachable binary record has structural log-cardinality $\varepsilon_0=\ln2$. Physical reset heat is assigned only when an implementation executes a registered cyclic reset, in which case $\varepsilon_{\mathrm{reset}}\ge H_q(P\mid R)$.

*   **$f_{RID}$:** (Lemma E.1) The maximal trace-distance contraction factor of the average 'Evolve' channel $\mathcal E_N$, with $0\le f_{RID}\le1$. A nonzero input-independent full-state refresh component gives $f_{RID}<1$. Independently, Proposition E.2a uses the log-cardinality $\varepsilon_0=\ln2$ of a completed binary reset-support factor to prove the support-dimension capacity deficit $C(\mathcal E_N)\le\ln d_0-\ln2$; this structural statement is not a claim of physical dissipation.

*   **False-Vacuum Fredholm Prefactor Certificate:** (Appendix U, Definition U.15d; Theorem U.15e; Corollary U.15f; Theorem U.15i.2) Certificate machinery specifying the seven explicit factors ($\mathcal J_{\mathrm{coll}}$, $|\mathcal A_{\mathrm{neg}}|$, $\mathcal A_{\mathrm{ghost}}$, $\mathcal A_{\mathrm{fv}}$, $\mathcal A_{\mathrm{meas}}$, $\det'_\zeta\mathcal H_{\mathrm{false}}$, $|\det''_\zeta\mathcal H_{\mathrm{bounce}}|$) needed for a theorem-level forward $A_{\mathrm{eff}}^{\mathrm{Fred}}$ on the Appendix U bounce branch. Theorem U.15i.2 identifies the determinant ratio as a relative Quillen-Fredholm norm; Theorem U.15e gates the audit on an accepted certificate, and performing the audit is a separate calculation. Without the certificate, the Appendix U working value remains a forward convention. Definition U.15e.1 and Theorem U.15e.2 add the dilatation negative-mode audit entry: a simple negative dilatation channel may contribute only as an evaluated determinant factor and never as a fifth zero mode.

*   **Finite-Budget Predictive Uncertainty Gate:** (Appendix K.3.6, Theorem K.3e, Corollary K.3f) The discrete operator-norm bound $\|\Pi_X F_\Lambda \Pi_P\|_{\mathrm{op}} \leq \sqrt{|X||P|/\Lambda}$ on the finite-budget Fourier transform restricted between configuration-support set $X$ and update-momentum-support set $P$ at SPAP cycle budget $\Lambda$. For $\Lambda\ge2$ and nonempty supports, equivalently, with $h=1/\Lambda$, $\delta_X=\log_\Lambda|X|$, and $\delta_P=\log_\Lambda|P|$, the norm is bounded by $h^{(1-\delta_X-\delta_P)/2}$. Corollary K.3f gives leakage probability at least $1-h^{1-\delta_X-\delta_P}$ for any unit state supported on $P$ when $\delta_X+\delta_P<1$.

*   **Finite-Channel Covariance:** (Appendix P, Definition P.16d.2.1; Theorem P.16d.2.2) Invariance of PU physical content under finite encoding, representation, protocol, observer, or resolution transformations that preserve retained response presheaves, retained maps, certificate commutativity, and descended costs.

*   **Finite Frameness Ledger:** (Appendix M, Definition M.6.13d; Theorem M.6.13e) Resource ledger for charges, phases, orientations, apparatus frames, and perspective labels. The frameness $\mathcal F_G(\rho)=S(\mathcal T_G\rho)-S(\rho)$ vanishes exactly for $G$-invariant states; zero-frameness labels are PPI/PCE-null.

*   **Finite Group-Step / Projection Distinction:** (Theorems Y.11.2a--Y.11.2b; Proposition Y.11.2c) Finite abelian symmetry can enter PU in two different ways. The group-step exponent law divides an exponential PCE complexity by $|G|$ under free-action, measure-preserving, regular-isotypic, nonzero-projection, and exponential-placement hypotheses. The isotropic projection law gives an RMS factor $|G|^{-1/2}$ under regular-fiber covariance hypotheses. These mechanisms are not interchangeable; the Majorana triality route is certificate-pending until the lift, fixed-locus, measure, character-selection, and exponent-placement audits close.

*   **Finite Nuclear Aggregate Operator Package:** (Appendix X, Definition X.8k.6a; Theorem X.8k.6b; Theorem X.8k.6c; Appendix T, Corollary T.79a.1; Theorem T.79a.2) Certificate machinery selecting the PCE-minimal self-adjoint colorless nuclear aggregate Hamiltonian with the retained Hilbert-space measure, many-body effective action, accepted boundary impedance record, exterior open-channel impedance, spin-current operators, transition operators, decay operators, uncertainty/covariance ledger, and overlap map to the elementary Appendix T parameter vector.

*   **Finite Predictive Budget:** (Appendix P, Definition P.16d.0.1) Finite protocol, outcome, coarse-graining, tolerance-quotient, and cost data $B=(\mathsf P_B,\Omega_B,q_B,\eta_B,\vartheta_B,\mathcal L_B)$ used to form the retained response quotient $Q_B(\mathcal A)$.

*   **Finite Predictive Horizon:** (Appendix P, Definition P.16d.9.1; Theorem P.16d.9.2) Kernel of accessible finite distinctions for a system at budget $B$. Distinctions inside the horizon cannot be reconstructed without a stronger budget or new separating protocol.

*   **Finite Response Alignment:** (Appendix P, Definition P.16d.15.1; Theorem P.16d.15.2) Alignment criterion for predictive systems requiring stable response compatibility under context translation, not equality of internal states.

*   **Finite Response Differential Character:** (Definition X.9.5f; Theorem X.9.5f.1) A finite protocol-cell character $(\chi,\omega)$ pairing retained cycle holonomies with integral-period curvature cochains. It packages flat frameness labels, gauge/calibration holonomy, perspective defects, Clausius defects, anomaly inflow, horizon/interface charges, and higher-form protocol charges into one exact obstruction spine after PPI quotienting.

*   **Finite-Response Bootstrap Strict-Gap Gate:** (Theorem X.8h.14) Spectra, thresholds, OPE coefficients, finite predictive amplitudes, Ward data, anomaly matching, and physical $S$-matrix identifications become joint theorem-level outputs only when they are registered projections of one finite bootstrap response datum with a strict PCE gap.

*   **Finite Spectral Calibration Datum:** (Definition V.3.11a) The finite record $\mathfrak S_*$ containing the finite calibration algebra $\mathcal A_*$, response-active spectral atoms $\Omega_*$, response-null quotient $\mathcal Q_{\mathrm{null}}$, invariant reference measure $\nu_*$, full-support feasibility witness $\nu_*^{\mathrm{fs}}$, independent moment constraints $m_a,c_a^*$, fixed operator symbols $\sigma_B$, unit bridges $s_B$, RG/threshold routes, circular-angle convention, and parent-sector overlap maps. If a response-active operator lacks a parent certificate, symbol, unit bridge, or certified projection to $\mathcal A_*$, its coefficient is not final-calibrated.

*   **Finite Tomita Standard Form:** (Appendix F, Definition F.10.4b.1a; Theorem F.10.4b.1b; Appendix O, Definition O.3a.2; Theorem O.3a.3) Finite-resolution modular algebra-state machinery. The local modular group, commutant, and thermal prediction time are determined by $(\mathfrak A,\omega)$.

*   **Functional Support, Protocol-Relative:** (Definition 5a; Proposition 5a; Corollary 5a.1) The subset of registered loop roles whose admissible isolated typed-null replacements change at least one law in a fixed finite protocol family. Response activity, failure of a qualifying margin, and collapse to a baseline are distinct. Support transports across carriers only when both original and null-replaced response laws are carried by the certified protocol and outcome equivalence.

*   **Fundamental Predictive Loop:** (Definition 4) The three operational phases Internal Prediction ($P_{int}$), Verification ($V$), and Update/Cycle ($D_{cyc}$), supported by the integrated Definition-5 capabilities $b_m,b_p,b_v$. The phase $D_{cyc}$ is initiated through $b_v$ and is not a fourth Definition-5 capability.

*   **$g_{\mu\nu}$:** (Theorem 45; Theorem 46; Corollary 46a; Appendix O) The effective metric tensor on the regular operational-continuum branch. Theorem 45 supplies the continuum metric structure and Theorem 46 only a uniform operational causal-speed upper bound; Lorentzian signature and local Lorentz kinematics require a separately attained frontier and the complete Corollary 46a/Appendix O promotion package.

*   **$\Gamma_0$ (Gamma-0):** (Definition 20) System-level **constant** Power Conversion Factor with dimensions of **Power** ($[E][T]^{-1}$), appearing in the PCE potential. It maps the marginal performance gradient $(\partial PP/\partial C)$ to an equivalent power rate in the Adaptation Driving Force $\Psi$. Constrained by thermodynamic limits (Theorem 20). **At equilibrium, parameters satisfy Equation (29)**, enabling empirical calibration of $\Gamma_0$ for a given MPU system.

*   **Gaussian Attractor:** (Corollary 22.1, Equation 37a) The local low-noise fluctuation distribution near a unique non-degenerate PCE attractor: $\xi=(x-x^*)/\sqrt\theta\Rightarrow\mathcal N(0,H^{-1})$ as $\theta\downarrow0$, with $H=\nabla^2V(x^*)\succ0$.

*   **Generation Vacua ($p_1,p_2,p_3$):** (Appendix R, Theorem R.5.1) On three disjoint compact sector manifolds, assume that each minimum set of $V_0$ is compact Morse--Bott with positive-definite normal Hessian, that the sector pinning function restricts to a Morse function with exactly one local minimum, and that the declared low-energy window has a positive $C^2$-stable gap from every other minimum. For sufficiently small $\eta>0$, $V_q=V_0+\eta h_q$ then has one nondegenerate minimum in each sector. Identifying these minima with fermion generations and mapping their distances to Yukawa couplings additionally requires the registered sector assignment and Gaussian-overlap hierarchy law; Theorem R.5.1 alone does not derive physical masses.

*   **Generative Non-Closure (GNC):** (Appendix A.6; Appendix P.16c) Conservative PU reading in which no trace-certified predictor has a total standard-sound same-cycle Boolean closure on a domain containing its targeted live diagonal sentence. A stable finite partial closure follows only for prediction-update cycles satisfying every hypothesis of Theorem A.6.4, including finite-budget retention, verification preservation, and the live-diagonal guard; the same sentence may remain externally or historically accessible. GNC adds no physical axiom and promotes no branch-conditional or certificate-pending physical claim.

*   **Geometric Regularity:** (Definitions 36–37, Theorem 43) Uniform polynomial volume growth together with the declared synthetic-curvature and variance controls. Appendix C excludes irregular competitors only under its edge-cost, synchronization/traffic, curvature-load, variance-transfer, convex-cost, and viability hypotheses. Effective Theorem D.3 makes every global minimizer regular only when every irregular admissible point has an equal-response regular competitor of strictly lower core potential. Low-noise concentration adds the compact irreducible detailed-balance diffusion hypotheses of Theorem D.5.

*   **Ginsparg-Wilson Chirality Gate:** (Appendix F.10.4a) Finite chiral-regulator rule replacing impossible naive exact lattice chirality with the Ginsparg-Wilson relation and finite index ledger $\operatorname{Tr}(\Gamma_5(1-a_{\mathrm{PU}}D/2))$. It supplies the finite-regulator bridge for chiral anomaly descent and the Appendix R/Y index statements.

*   **Global Strict-Certificate Ledger:** (Appendix P, Convention P.14.1k; Theorem P.14.1k.1; Corollary P.14.1k.2) Sector-by-sector audit assigning each major PU output its selected representative, local strict PPI/PCE certificate or theorem source, and closure status. Theorem P.14.1k.1 selects a compatible sector tuple under its acceptance and overlap hypotheses. A single ambient global realizer is an additional claim requiring a declared global-realizer space, a restriction map to the compatible sector-record space, a nonempty fiber over the selected tuple, and a singleton fiber for uniqueness up to global response equivalence.

*   **Conditional Golay Bridge:** (Appendix R, Lemma R.4.5; Proposition R.4.2a; Theorem R.4.9) The code-to-lattice transfer requires a registered finite-quadratic-module isometry, maximal isotropic subgroup, glue representatives, and coset-minimum ledger. Hamming distance transfers to rootlessness only when Equation (R.4.2a.1) is certified.




*   **Classical Golay Ledger ($\mathcal G_{24}$):** (Appendix Z, Theorem Z.13; Appendix R, Theorem R.4.4) This binary subspace has length $24$, dimension $12$, and least nonzero Hamming weight $8$. Standard distance rules permit seven-symbol detection and guarantee correction through weight $3$. No physical or quantum protection follows until an encoder, channel, syndrome instrument, and recovery map are registered. A maximal CSS state from the self-dual code has no logical-qubit distance claim and is not thereby a physical vacuum.



*   **Glueball Shell-Assignment Branch:** (Theorem Z.8h; Theorem Z.8h.3; Remark Z.8h.4) Prescribed map from $J^{PC}$ labels to Leech shells and a spin-corrected mass readout. It is retired against the stated pure $SU(3)$ Yang–Mills benchmark. The numerical scale inventory $(a,b,M,|v|_{\min}^2,d_{\mathrm{Golay}})=(2,6,24,4,8)$ remains, but no physical $J^{PC}$ assignment follows from that inventory.

*   **Golay Noise Spectroscopy:** (Appendix Z, Corollary Z.13b.1, Definition Z.13b.2, Theorem Z.13b.3, Corollary Z.13b.5; Section 13.9, Protocol 4.4) Conditional residual-noise branch on which irreducible correlated 24-mode residual syndromes decompose into Golay shells after ordinary device-local noise is separated. The leading octad statistic satisfies $\mathcal R_8=1+968\eta_8$, the octad incidence sequence is $253:77:21:5:1$, and controlled weight-4 decoder-boundary probes decompose into $1771$ six-tetrad sextets.

*   **Golay Octad Incidence Certificate:** (Theorem Z.13c.3; Corollary Z.13c.4) The $759\times24$ octad incidence matrix has $H^TH=176I+77J$, real rank $24$, binary rank $12$, and row space and parity kernel equal to $\mathcal G_{24}$. Its biregular Ramanujan spectrum is a finite graph certificate and has no arithmetic or fine-structure-constant implication.

*   **Golay Weight-Moment Reference Record:** (Theorem Z.13c.1; Corollary V.3.11h) The uniform-codeword reference record on $\mathcal G_{24}$ with weight pushforward $(1,759,2576,759,1)/4096$, binomial/Gaussian central moments through order $7$, and first order-$8$ deviation $239085/2$. It fixes only the reference-measure gate on the Golay carrier, not the full calibration datum.

*   **$\mathcal H_0$:** (Proposition 4) The complex Hilbert carrier for the MPU state $|\psi(t)\rangle$. On Theorem 15's (O1)–(O3), (FC) realization class, mutually perfectly distinguishable representatives of the eight visited contexts imply $d_0=\dim\mathcal H_0\ge8$. Equality requires the additional minimal-branch hypotheses of Theorem Z.2; it is not fixed by SPAP alone.

*   **Hadamard-PCE State Gate:** (Appendix K.10.4a–K.10.4c) Regular AQFT admissibility condition requiring finite Hadamard-subtracted ultraviolet PCE gate cost. Hadamard regularity is a prerequisite for point-split stress-energy and Wick-power renormalization on the continuum branch; KMS structure, Clausius response, horizon saturation, and metric response require independent certificates.

*   **Higher-Form Predictive Ledger:** (Appendix X.9.5d) Extension of the finite PU obstruction complex to line, surface, and higher-dimensional protocol operators. Higher-form labels with no extended-interface action are PCE-null; active classes obey the same exactness rule as ordinary anomaly and boundary ledgers.

*   **Homogeneous-Sector Zeta Reduction Certificate:** (Theorem X.9.6g.2a) A forward spectral certificate for compact homogeneous sectors $G/H$ in which the group pair, bundle representation, invariant operator symbol, zero-mode rule, finite-part scheme, normalization, and tail certificate reduce the retained heat/zeta/determinant record to representation-theoretic Barnes/Shintani-type lattice zeta sums. It is a reduction format, not a numerical closure by itself.

*   **Horizon Constant ($K_0$):** (Theorem 15) The least visited-context log-capacity for the stated SPAP encoding within the realization class satisfying (O1)–(O3) and (FC). In that class, $N_{\mathrm{vis}}^{\min}=8$ and $K_0=\log_2N_{\mathrm{vis}}^{\min}=3$. The same register size separately admits a basic super-chance mode on $\mathcal E_{\mathrm{basic}}(\gamma)$. This is not an absolute complexity floor for every predictor. Corollary 3 gives $C_{op}\ge K_0$ only under all its hypotheses: every $\mu\in\mathcal Q$ satisfies (O1)–(O3) and (FC), lies on the Hilbert-carrier branch with the eight registered contexts represented by mutually perfectly distinguishable states, and obeys the declared complexity-capacity bridge $C_P(\mu)\ge\log_2 d_0(\mu)$.

*   **Horizon Structural No-Deletion Closure:** (Appendix E, Definition E.9.5d; Theorem E.9.5e; Definition E.9.5f; Theorem E.9.5f.1; Corollary E.9.5e.1) Closure of the retained horizon structural-conservation layer. Injective microscopic update on retained finite-response classes prevents deletion in the retained PU state. Exterior recovery from the coarse horizon algebra is theorem-level only after an exterior recovery sufficiency certificate is accepted; Page-curve behavior retains the scrambling-branch status unless an accepted horizon design certificate is supplied, with the Golay-expander route given by Definition K.3d.4a and Theorem K.3d.4b.

*   **Hypothesis 1 (Nominated MPU Reality Model):** (Section 7.1) Nominates a network of interacting MPUs governed by POP/PCE and Dual Dynamics as PU's foundational physical-realization branch. Finite implementation and response-null compression motivate the nomination, but neither the Cogito nor the current PPI/PCE results prove that this network is the realized substrate.

*   **Hypothesis 3 (CC Influence Mechanism):** (Section 9.4.1) Proposes that Consciousness Complexity (CC) influences quantum outcomes by modulating the parameters of the 'Evolve'/ND-RID process via context-dependent physical fields. The electromagnetic channel dominates ($\mathcal{R} \sim 10^{36}$, Proposition L.5) with both EM and gravitational channels unified through temporal wave modulation (Theorem L.8). Appendix S supplies a gravitational CC ceiling only on its declared context-power, retained-energy/perfect-fluid, weak-field geometry, and calibrated phase-or-dephasing response branches; the local CPTP branch preserves Bob's marginal, and no universal gravitational CC ceiling follows from PCE alone.

*   **Identifier and Notation Collision Discipline:** (Appendix P, Convention P.14.1j) Constraint that any relabeling, theorem promotion, or notation split must preserve unique identifiers within each local numbering family and route every occurrence of a split symbol to its structural, physical, branch, model, or validation meaning. Required so that citations determine unique nodes in the proof graph and formulas determine which quantity is being summed.

*   **Finite-Resource Profile of a Hidden-Variable Completion:** (Definition 14.4b.1; Theorem 14.4c) Finite record $(\mathcal Q_h^{\mathrm{fd}},\mathcal Q_h^{\mathrm{fr}},\bar{\mathcal Q}_h^{\mathrm{diag}},\mathrm{ns},\mathrm{aqft},\mathrm{art},\mathrm{op},\mathrm{rn})$ classifying a hidden-variable completion by finite depth, finite resource, pre-commit communication closure, sealed-core no-signaling, AQFT/locality compatibility, artifact-identification surplus, operational response difference, and response-null status. Theorem 14.4c separates temporal inadmissibility, artifact surplus, SPAP-excluded self-access, no-signaling-exposed query access, AQFT/locality-incompatible profiles, local deviating model layers, finite-resource query failure, and response-null inaccessible or operationally equivalent completions.

*   **Information:** (Definition 1) A physically instantiated, substrate-independent pattern or correlation structure with objective potential to yield a positive expected improvement in predictive quality for a task-suitable system and admissible model. Suitability requires enough task-specific resources to realize the improvement; Definition 1 imposes no universal $K_0$ floor. Theorem 15's $K_0$ applies only to its SPAP register branch and reaches $C_P$ only through Corollary 3's bridge.

*   **Instanton Complexity ($\kappa$):** (Appendix U, Theorem U.13; Theorem U.13b; Theorem U.16; Theorem U.8c) Dimensionless semiclassical action exponent controlling vacuum tunneling suppression in the cosmological-constant relation $\Lambda L_P^2 = 8\pi A_{\text{eff}} e^{-2\kappa}$. In Appendix U, the value $\kappa_{\mathrm{ref}}=141.5$ is the Appendix U five-mode reference exponent, while Theorem U.8c shows that the pure-coordinate dilatation tangent needed to realize that branch has strictly negative Hessian quadratic form and is not in the Hessian kernel for the current Definition U.4 continuum action; the theorem does not identify that tangent as a Hessian eigenvector. Theorem U.13 further shows, under its sampled-sector exactness hypothesis, that the sampled translation+dilatation restriction of the discrete Hessian has no fifth zero mode. After the corrected Definition U.6 normalization, Theorem U.13b gives the full discrete four-mode closure under the explicit false-vacuum spectral hypotheses stated there, so the corresponding four-mode branch value is $\kappa=142$ within that theorem. Corollary U.15b then distinguishes the observational inversion $A_{\text{eff}}^{(\text{obs})}=0.917\pm0.016$ on that reference branch from the Appendix U working value $A_{\text{eff}}=0.923\pm0.011$ used in the forward reference evaluation. Moonshine character coefficients such as $196884$ are not inputs to this exponent ledger. (Do not confuse this $\kappa$ with the gauge-field normalization factors $\kappa_{\mathrm{bulk}}, \kappa_{\mathrm{eff}}$ used in Appendix Z.)

*   **Institutional Predictive Code:** (Appendix P, Definition P.16d.14.1; Theorem P.16d.14.2) Network-level record, verification, correction, and update machinery preserving knowledge exactly when correction maps descend to identity on the retained response quotient.

*   **Interface Correction ($\delta\kappa$):** (Appendix Z, Section Z.17, Theorem Z.17) On the bounded-$C^2$ interface-response branch, the first-order correction to the bulk normalization is $\delta\kappa=-c_{\mathrm{int}}(a/d_0)(u^*/\sqrt{K_0})+O(u^{*2})$, where the microscopic interface-response coefficient satisfies $c_{\mathrm{int}}>0$. The response-ordering hypothesis of Theorem Z.16 determines the sign. On the additional unit-interface-response branch $c_{\mathrm{int}}=1$, $a/d_0=1/4$, $K_0=3$, and $u^*=8^{1/24}-1$, the first-order term is $-0.01306366595\ldots$ and $\kappa_{\rm eff}=0.9869363340\ldots+O(u^{*2})$.

*   **'Internal Prediction' Phase ($P_{int}$):** (Definition 26) The deterministic, reset-free predictive generation phase of MPU dynamics between 'Evolve' events. On the Hilbert/Born branch satisfying Theorem 8.7, this phase is implemented by unitary Schrödinger evolution (Equation 43).

*   **Intervention Budget Functional:** (Appendix P, Definition P.16d.16.1; Theorem P.16d.16.2) PCE objective for interventions combining intervention cost, expected predictive loss, surplus disruption, and constraint-violation penalty.

*   **$k_{PP}$ (Performance Scale):** Positive constant that rescales $PE$ in Definition 7 via $PP(t)=\frac{1}{1+k_{PP}PE(t)}$. If $PE$ has units, then $k_{PP}$ carries reciprocal units so that $k_{PP}PE$ is dimensionless.

*   **Källén-Lehmann PPI Spectral Gate:** (Appendix F.10.4c.4) Positive spectral-measure representation for two-point functions of PPI-observable operators. Particle labels are operational only when they appear in the positive spectral response of a gauge-invariant or PPI-descended operator.

*   **$\kappa_C$ (Reduced Curvature):** (Corollary D.5b.1) The second derivative of the reduced effective potential at the PCE optimum, $\kappa_C := V''_{eff}(C^\star) = -\Psi'(C^\star)$. It is positive under DSC and measures the local stiffness of the complexity optimum.

*   **$\kappa_{\mathrm{eff}}$:** (Theorem 19, Remark 4) Dimensionless efficiency parameter governing the relationship between complexity and performance in the Law of Prediction (Equation 22). Related to equilibrium cost/benefit ratios (Equation 32).

*   **$\kappa_r$:** (Theorem 33) The positive constant ($\kappa_r > 0$) appearing in the Reflexivity Constraint, quantifying the fundamental trade-off between information gain $\Delta I$ and minimum state disturbance $\Delta S_{min}/k_B$. Arises from $\varepsilon_{\mathrm{phys}}\ge H_q(P\mid R)\quad(\text{registered reset branch; a positive floor requires }H_q(P\mid R)\ge h_{\min}>0)$.

*   **Kissing Number ($K(D)$):** (Appendix Z, Definition Z.7, Section Z.10) The maximum number of non-overlapping unit spheres that can simultaneously touch a central unit sphere in $D$-dimensional Euclidean space. Key values include $K(3)=12$, $K(4)=24$ (the regular $24$-cell), and $K(8)=240$ (the $E_8$ root configuration). Definition Z.9a requires a faithful injection of $M=24$ retained response labels into distinct tangent-shell cells, hence $24\le K(D)$. Theorem Z.11 excludes $D\le3$, uses the regular $24$-cell as a feasibility witness in $D=4$, and invokes a strict surplus-dimension cost to select the least feasible dimension. It requires no global saturation equality. Interpreting shell cells as distinct neighboring MPUs requires a separate population-graph realization.

*   **Kleiber Scaling (PU conditional biological branch):** (Theorem P.8.9a.12.7) A terminal-invariant, space-filling, area-preserving transport network with mass proportional to network volume gives $B\asymp M^{D/(D+1)}$, hence $B\asymp M^{3/4}$ when $D_{\mathrm{space}}=3$.

*   **KMS (Kubo-Martin-Schwinger) Condition:** (Theorem G.1.9.5) Every faithful state obeys a modular KMS identity relative to its own modular flow. Physical thermal/KMS status relative to registered time requires the independent QDB/physical-time or complete-passivity certificate of Theorems G.1.9.3 and G.1.9.3c; PCE alone does not supply it.



*   **Knowledge Interface Area Law:** (Appendix P, Definition P.16d.20.1; Theorem P.16d.20.2) Finite-capacity upper bound on cross-boundary knowledge growth. In one update step, transferable distinguishable response information across a cut is bounded by $\operatorname{Cap}_B(\partial A)$.

*   **KWW/Stretched-Exponential Relaxation:** (Theorem P.8.9a.12.4) A stable-subordinated positive mixture of exponential PCE relaxation modes gives $\Phi(t)=\exp[-(t/\tau)^\alpha]$ for $0<\alpha\le1$.

*   **Landauer Horizon Spectroscopy:** (Appendix Q, Definition Q.0.7u, Proposition Q.0.19, Proposition Q.0.20, Corollaries Q.0.20a–Q.0.20c; Section 13.8.4) Conditional horizon-transfer branch on which resonant positions satisfy $\operatorname{dist}_{2\pi}(x,N\ln2)\le\varepsilon_{\mathrm{peak}}$ around the exact central labels $[N\ln2]_{2\pi}$.

*   **Dense-Subgroup Invariance Extension:** (Appendix Q, Theorem Q.0.7d2; Corollary Q.0.7d3) If a topological finite-response configuration space independently carries a continuous $U(1)$ action and a continuous response functional is invariant under the registered dense cyclic subgroup $G_L=\{e^{iN\ln2}:N\in\mathbb Z\}$, continuity extends that invariance to all of $U(1)$. A local Noether current additionally requires a differentiable local action, an admitted local gauge representation, and the standard boundary hypotheses; subgroup density supplies none of those data.
*   **Landauer Phase-Grid Ledger:** (Definition Q.0.7u; Proposition Q.0.20b.1) Conditional horizon closed-loop transfer-branch record for the phase positions $x_N=[N\ln2]_{2\pi}$ in $x=\hbar\omega/(k_BT_H)$. The ledger fixes transcendence/aperiodicity, the continued-fraction prefix of $\ln2/(2\pi)$, convergent near-recurrences, finite discrepancy bounds, and two-/three-gap statistics; it does not fix amplitudes, widths, greybody factors, or the transfer-map derivation.

*   **Active Verification Pointer (historically “Landauer Pointer”):** (Appendix Z, Theorem Z.1, Section Z.2.3) The least physical carrier of the sharp retained match/mismatch quotient on the accepted Hilbert branch. The two responses give $a\ge2$; the capacity gate $\ln a\ge\varepsilon_0=\ln2$, response quotient, attained-rank premise, and strict no-surplus cost select $a=2$. A Landauer heat statement follows only if this carrier is subsequently subjected to a registered cyclic noninjective reset satisfying Theorem 31.

*   **Law of Prediction:** (Definition 19; Theorem 19; Equation 22) On the continuous operational-complexity interpolation for a declared task and target scale, assume the normalized residual gap is continuous and composes multiplicatively under independent relative-budget stages. Writing $\overline{PP}$ for the analytic extension to $C=C_{op}$, $g(x+y)=g(x)g(y)$ gives $g(x)=e^{-\kappa_{\mathrm{eff}}x}$ and
$$
\overline{PP}(C,\hat C_{\mathrm{target}})
=
\beta-(\beta-\alpha)
\exp\!\left[
-\kappa_{\mathrm{eff}}
\frac{C-C_{op}}{\hat C_{\mathrm{target}}}
\right].
$$
Physical $PP$ is the restriction to $C>C_{op}$. The $\alpha$-anchored equality at $C=C_{op}$ is a nonattained analytic boundary unless the law is indexed to a different task, score, or evaluation window. It cannot simultaneously be the performance of a same-task attained MPU whose qualifying margin above the random baseline is strictly positive.

*   **Leech Lattice ($\Lambda_{24}$):** (Appendix R, Section R.4.2) Achieves the optimal sphere packing density in 24 dimensions (Cohn et al. 2017), with kissing number 196,560 and minimum squared Euclidean norm $4$ (hence minimum Euclidean length $2$ and no norm-$2$ roots). Contains the sublattice $\sqrt{2}E_8 \oplus \sqrt{2}E_8 \oplus \sqrt{2}E_8$ with index $2^{12}$, providing a geometric three-fold structure compatible with the generation analysis of Appendix R. Its dimension 24 coincides with the interface mode count $M = 24$ (Theorem Z.5).

*   **Light Sublattice ($L_{\rm light}$):** (Appendix R, Definition R.3.2, Proposition R.3.3) The integer kernel $L_{\rm light} := \ker_{\mathbb{Z}} C \subset \mathbb{Z}^7$ of the charge map matrix $C$ encoding $SU(3) \times SU(2)$ Cartan generators. Contains topological sectors neutral under non-Abelian gauge groups. All elements have trivial $SU(3)$ center charge: $\tau(q) = 0 \in \mathbb{Z}_3$ for $q \in L_{\rm light}$.

*   **Log-Flat Relaxation Spectrum:** (Theorem P.8.9a.12.3) A mixture of exponential modes with rate measure $A\,d\lambda/\lambda$ over a broad rate window gives an intermediate $1/f$ spectrum $S(\omega)\sim\pi A/\omega$.

*   **Log-Periodic Correction:** (Theorem P.8.9a.12.6) Discrete scale covariance $F(\lambda x)=\lambda^\alpha F(x)$ gives $F(x)=x^\alpha P(\log x/\log\lambda)$ with period-one $P$ and Fourier modes periodic in $\log x$.

*   **Logical Indeterminacy:** (Definition 12) Two distinct class-level limitations. SPAP excludes a universally exact predictor on its stated diagonal-closed Property-R class. RUD excludes a total uniform decider for the fixed property $\mathsf{TERM}$ on every effectively coded pointed RID class containing the registered-clock simulations; its probabilistic extension has the additional finite-runtime and effectively listed rational finite-support hypotheses of Theorem A.2.4. Neither result implies eventwise stochasticity. On a separately supplied stochastic complex-Hilbert branch, Theorem 28a represents normalized noncontextual finite-additive effect weights by the Born trace rule.

*   **Lorentz Six-Generator Source:** (Corollary G.10.5.1) The real Lie-algebra count $6_{\mathrm{Lorentz}}=\dim_{\mathbb R}\mathfrak{so}(1,3)$ comes from the active rank-2 spinor determinant form on the Lorentzian branch via $SL(2,\mathbb C)/\{\pm I\}\cong SO^+(1,3)$. It is distinct from the inactive complex rank $\dim_{\mathbb C}\mathcal B=6$ used by the gauge-capacity ledger.


*   **LSM-PCE Ingappability Gate:** (Appendix Z.8d.2–Z.8d.4) Regular crystallographic branch audit: nontrivial unit-cell projective or fractional symmetry data forbid an IR phase that is simultaneously symmetric, short-range-entangled, nondegenerate, fully gapped, and uncompensated.

*   **Marked Golay-Leech Interface Frame:** (Definition G.8.4f.3) The systematic Golay coordinate frame, signal/parity split, octad trio, and canonical half-swap $J_0=(1\,13)(2\,14)\cdots(12\,24)$ used to define a marked threshold branch. Proposition G.8.4f.5 shows that this marking is additional branch data rather than an unmarked uniqueness consequence.

*   **Mass Hierarchy Invariant ($\mathcal R$):** (Appendix R, Definition R.5.1) Once a path-additive distance triad is registered, $\mathcal R=\ln(m_3/m_1)/\ln(m_3/m_2)=d_{31}^2/d_{32}^2$ cancels any common leading coefficient. This algebraic cancellation does not derive the physical distance assignment, fourth-order coefficient, or remainder. The charged-lepton and neutrino values therefore retain their Appendix T model and anchoring conditions.



*   **Master Branch-Exclusion Ledger:** (Appendix P, Convention P.14.1h) Single dependency table recording, for every major PU output, its claimed value, local status, principal alternatives, exclusion or quotient mechanism, response-null or PCE-dominated surplus, and residual open status. Makes theorem-level, branch-level, certificate-level, model-level, and open-bridge claims visible in one ledger, with the alternative-by-alternative exclusion mechanism cited by theorem.

*   **Matched Dark-Sector Discriminator:** (Definitions I.12a.1-I.12a.2) A likelihood-equivalent residual comparison between PU scaled gravity and NFW dark-matter fits using the same covariance, nuisance parameters, baryonic preprocessing, and masking conventions.

*   **Matched-Encoding Yoked-Receiver Audit:** (Proposition M.10.9d; Protocol M.10.12; Protocol Convention L.8.0) A crossed engineered-receiver audit in which the same serialized pattern is delivered to both arms. Pattern-only functionals are thereby held fixed, while typed target binding, model access, cost units, initial-state restoration, and receiver-exchange controls remain independent requirements. A diverging contrast requires a separately realized finite-$\mu$ ladder, a uniform reduction certificate, and a bounded-control cost certificate; a finite audit instead uses Corollary B.2.2's all-path upper-bound criterion.

*   **Measurement:** (Definition 30; Definition 28; Theorem 31) The process of actualizing quantum possibilities through MPU dynamics. A measurement architecture that uses a declared binary reset support carries the structural value $\varepsilon_0=\ln2$. If its record is physically reset under Definition 28, the reset obeys $\varepsilon_{\mathrm{reset}}\ge H_q(P\mid R)$; no reset heat is assigned to every measurement cycle without that implementation record.

*   **Meta-Theoretic Comparative Adequacy:** (Appendix P, P.14.9; Section 14.6.8; Section 15) Separation of object-level finite-record claims from meta-level methodology. The comparison criterion is predictive yield per structural description cost $K_{\mathcal E}(\mathcal T)$ on a shared empirical domain. The criterion is not a certificate gate and cannot repair failed predictions or missing finite records.

*   **Minimal Family-Charge Selection:** (Appendix R, Definition R.8.5a; Theorem R.8.5b; Remark R.8.5c; Corollary R.8.5d) PCE minimal-selection audit in the integer $U(1)_F$ family-charge class. The unique minimal-cost orbit satisfying linear anomaly cancellation, cubic anomaly cancellation, non-triviality, CP-active distinctness, and PCE cost monotonicity is $\{a,-a,0\}$, giving $N_g=3$. The statement is not an exhaustive classification of all larger anomaly-free charge multisets; those are PCE-demoted or different response-active branches.

*   **Faithful Mode--Cell Realization:** (Definition Z.9a; Theorems Z.10--Z.11) On the registered tangent-cell branch with a verified response-preserving injection, $D_{\min}=\min\{D\in\mathbb N:K(D)\ge24\}=4$. Golay coding and Leech gluing are separate branches and are neither premises nor consequences of this finite embedding theorem.



*   **Model-Indexed LITE:** (Appendix A.5.6a) A bounded proof search through a predictor-indexed admissibility relation. In the labeled version,
    $$
    \operatorname{LabProof}_{\mathcal F}(p,e,1)
    \Longleftrightarrow
    \operatorname{Proof}_{\mathcal F}(p,e),
    \qquad
    \operatorname{LabProof}_{\mathcal F}(p,e,0)
    \Longleftrightarrow
    \operatorname{Proof}_{\mathcal F}(p,\operatorname{Neg}(e)),
    $$
    and $\operatorname{LITE}^{\mathrm{act}}_B$ or $\operatorname{LITE}^{\mathrm{hist}}_B$ returns a bounded admitted labeled proof, or $\bot$ if none is admitted. For $E_{B,t}$, active register-coherent access misses every bound, while faithful historical access recovers the correct label $y^*_{B,t}$ for sufficiently large bounds.

*   **Modular Chaos Bound:** (Appendix F.10.4b) KMS analyticity and finite predictive capacity bound exponential OTOC growth by $\lambda_L\le2\pi/(\beta\hbar)$ on branches satisfying the standard scrambling-window hypotheses.

*   **Modular Recoverability Gravity Gate:** (Corollary 12.5.3j) On the regular KMS/AQFT branch, the recoverability defect $D(\rho_D\Vert\omega_D)-D(\Phi_D\rho_D\Vert\Phi_D\omega_D)$ is nonnegative. The reversible Einstein equation is the zero-curvature branch of the local null recoverability ledger; positive retained curvature enters as an explicit non-equilibrium slack tensor.

*   **Module Decomposition Theorem:** (Theorem G.8.4b, Appendix G) In the finite-response positive-marginal capacity regime of Equation (G.8.5), assume the full-block frame-family rule: every nontrivial block retains exactly its full $\mathfrak{su}(n_i)$ factor, no proper connected subgroup substitution or factor deletion is admitted, and only response-active center directions may be omitted to obey capacity. With response-null exact global phases quotiented, the unique capacity-saturating direct-sum decomposition of the inactive fiber $\mathcal B=\mathbb C^6$ within that declared family is
    $$
    \mathcal B=\mathbb C^3\oplus\mathbb C^2\oplus\mathbb C^1.
    $$
    The derivation uses exhaustive block-partition analysis under: (i) the Lagrangian capacity bound $n_G\leq12$ (Theorem G.8.2e); (ii) the positive-marginal saturation rule selecting admissible block-frame algebras with $n_G=12$ whenever attainable; (iii) the full-block frame-family rule stated above; (iv) faithfulness; and (v) determinant-compatible anomaly/hypercharge constraints. Under these conditions, the Standard Model gauge algebra
    $$
    \mathfrak{su}(3) \oplus \mathfrak{su}(2) \oplus \mathfrak{u}(1)
    $$
    is the unique determinant-compatible block-frame solution (Corollary G.8.4c). This is not a classification of all compact connected subgroups of $U(6)$; irreducible tensor-product embeddings are outside the block-frame admissible family. Theorems G.8.4e and G.8.4e.1 prove that this fiberwise $(3,2,1)$ decomposition does not globalize on the bare universal quotient bundle $Q \to \mathrm{Gr}(2,8)$ and that the minimal flag lift $\widetilde X = \mathrm{Flag}_{1,2,3}(Q)$ is the corresponding universal ordered-splitting lift. The quantitative gauge-threshold sector is therefore formulated on $\widetilde X$, where $\pi^*Q$ tautologically splits. Definition G.8.4f.3 fixes a marked Golay-Leech half-swap representative available to the canonical marked branch, while Proposition G.8.4f.5 shows that the unmarked Golay-Leech structure does not force a unique $\mathcal J_G$.

*   **Mosco--Bochner Local-Net Bridge Certificate:** (Appendix F, Definition F.0c; Theorem F.0d) Certificate machinery combining Mosco convergence, Bochner control, noncollapse, generator convergence, inductive-limit compatibility, Lieb--Robinson normalization, and spectral control to produce a stable local $C^*$-net and contraction semigroup. It is not a Haag--Kastler/Wightman promotion certificate; that requires Definition F.0h.

*   **MPU (Minimal Predictive Unit):** (Definition 23) The hypothesized fundamental constituent of the physical model, defined as a system possessing Predictive Physical Complexity exactly equal to the Operational Threshold $C_{op}$. It is the simplest nontrivial finite physical representative of the Cogito-derived predictive loop under PPI admissibility and PCE no-surplus selection, and operates the adaptive Fundamental Predictive Loop under Dual Dynamics.

*   **Multiplicative PCE Noise Hypothesis:** (Hypothesis P.8.9a.11.4) The conditional population-level assumption that near-equilibrium complexity excess follows $d\nu=(-k\nu+m)dt+\sigma\nu dW$ above an operational floor. It yields the Pareto tail in Theorem P.8.9a.11.5 but is not derived from POP/PCE alone.

*   **ND-RID (Non-Deterministic Reflexive Interaction Dynamics):** (Definition 6) The formalism describing the probabilistic, irreversible 'Evolve' interaction process, where state transitions depend reflexively on stochastic outcomes. Characterized by $\varepsilon_0=\ln2$, $\varepsilon_{\mathrm{phys}}\ge H_q(P\mid R)\quad(\text{registered reset branch; a positive floor requires }H_q(P\mid R)\ge h_{\min}>0)$, the completed reset-support capacity deficit of Proposition E.2a, and $f_{RID}<1$ on refresh/minorization branches satisfying Lemma E.1.

*   **ND-RID Process Tensor:** (Appendix M, Definition M.6.14a; Theorem M.6.14b; Corollaries M.6.14c–M.6.14d) Finite multi-time Choi object representing measurement histories, memory, causal-order resources, post-selection, and weak-probe protocols under ND-RID. Positivity and comb trace constraints enforce no future-to-past signaling: future records can condition retrodictive descriptions, but cannot alter unconditioned earlier marginals.

*   **Normalized Selection–Bayes–Multiplicative-Weights Correspondence:** (Appendix P, Theorem P.8.9a.6a; Remark P.8.9a.6b) On a finite type simplex and the domain $Z(p,w)=\sum_jp_jw_j>0$, the update $p_i^+=p_iw_i/Z(p,w)$ is simultaneously normalized selection, a Bayesian posterior for likelihoods proportional to $w_i$, and multiplicative weights with $w_i=e^{-\eta\ell_i}$, using the extended-loss convention $\ell_i=+\infty$ when $w_i=0$ and $e^{-\eta\infty}=0$. Mutation composes an additional stochastic kernel, general frequency dependence preserves only the pointwise identity, and the equation correspondence alone establishes neither PU functional-role identity nor SPAP self-reference.

*   **No-Overclaim Discipline:** (Appendix P, Convention P.14.1l) Status-propagation rule stating that no sector listed in Convention P.14.1k may inherit theorem-level numerical status from a parent theorem unless every finite-response map used by that sector also has a theorem-level certificate or a closed-status entry. Status propagation is by the meet rule on the dependency graph of Convention P.14.1k.

*   **Observer Calibration Kernel:** (Appendix P, Definition P.16d.8.1; Theorem P.16d.8.2) Finite stochastic natural transformation between predictive systems' response presheaves. Reproducibility is the existence of a common calibrated response class.

*   **Off-Branch Entropy-Flow Residual Decomposition:** (Appendix N, Corollary N.4.2; Remark N.4.2.1) On the saturated-activity completed-reset branch only, $\mathcal R_N$ decomposes into registered boundary-utilization, physical-equilibrium excess-production, and implementation-overhead terms. Unsaturated activity is a separate signed coordinate and carries no unconditional $\mathcal R_N\ge0$ conclusion; equality requires the joint saturated-activity/boundary, physically certified equilibrium, overhead-free branch.



*   **Ontology Audit Gate:** (Appendix P, Definition P.16d.17.1; Theorem P.16d.17.2) No-surplus test for proposed entities, branch labels, hidden variables, continuum refinements, or interpretations. A proposal is retained only by response change, retained-map change, cost reduction, certificate role, or finite branch-degeneracy record.

*   **Operational Alphabet Capacity:** (Appendix Z, Definition Z.4, Theorem Z.6) The maximum information $\ln d_0$ that can be reliably encoded and retrieved in a single MPU cycle when rank-raising instruments are available. Capacity saturation at the PCE-Attractor ($\mathcal{I}(u^*) = \ln d_0$) determines the bare coupling $u^* = d_0^{1/M} - 1 = 2^{1/8} - 1$.

*   **Operational Conservative-Projection Principle:** (Theorem P.14.1k.0a) PU results that use only operational core data are invariant under interpretive changes preserving those data up to natural isomorphism. Interpretive claims that change no finite response data cannot change theorem-level predictions; claims that change finite response data are branch hypotheses.

*   **Operational Latent Mathematics:** (Appendix P.6.1) The PPI-physicalizable but not yet actualized sector
    $$
    \mathfrak W_{\mathrm{OLM}}(t)=\mathfrak P\setminus\mathfrak A(t),
    $$
    together with the predictor-relative sector
    $$
    \mathfrak W_{\mathrm{OLM}}(S,t)=\mathfrak P\setminus\mathfrak E_S(t).
    $$
    These are mathematical structures eligible for finite-cost physicalization or access but not yet actualized or not yet accessible to the specified predictor.

*   **Operational Naturalness:** (Appendix P, Definition P.16d.4.1; Theorem P.16d.4.2) Stability of a retained parameter under PRG quotient flow with bounded transition description. Precision that changes no finite response, retained map, certificate role, or cost is operational fine-tuning and is removed.

*   **Operational Paley-Wiener Sector:** (Appendix F.10.4a) The finite spectral-band sector $PW_\Omega(L_{\mathrm{PU}})=\operatorname{Ran}\mathbf 1_{[0,\Omega]}(L_{\mathrm{PU}})$ of the propagation-cost Laplacian. Below operational bandwidth, cell averages form a stable reconstruction frame on this sector.

*   **Operational Qualia Metric:** (Appendix P.16b.12 and P.16b.14) A finite retained-test pseudometric on organismic configurations, defined by positive weighted sums of response-probe distances and descended to a metric on the zero-distance quotient (Theorem P.16b.12.10). On the self-organization branch, operational qualia are active configurations registered by the organismic response structure rather than extra decorations attached to configurations (Theorem P.16b.14.10).

*   **Operational Record Consensus:** (Theorem M.4a) The finite record-merging step after perspectival actualization. Born actualization supplies the prior $p(k)$; finite record channels contribute likelihoods $L_r(k)$; the unique KL-minimizing merged record distribution is $q^*(k)\propto p(k)\prod_rL_r(k)^{w_r}$. Byzantine record robustness is a finite-record supermajority condition, not a separate collapse postulate.

*   **Ordered $(12,24)$ Bridge:** (Appendix Z.5) The componentwise ledger equivalence
    $$
    (k,M)_{\mathrm{PU}}
    =
    (12,24)
    =
    (\operatorname{weight}(\Delta),\operatorname{exp}_{\eta}(\Delta))_{\mathrm{Ramanujan}}
    =
    (\operatorname{weight}(\Theta_{\Lambda_{24}}),\operatorname{rank}(\Lambda_{24}))_{\mathrm{Leech}}.
    $$
    It is not a bare numerological occurrence of $24$; admissibility requires preservation of the ordered pair and the doubling relation $24=2\cdot12$.

*   **Paradox-Avoidance Residue:** (Section 14.5.8, Definition 14.5.8) A hypothetical observer-indexed correction to local outcome statistics. The *active tier* is a nominated Appendix L response law for which Theorems 39--42 provide conditional bounds but no realized carrier or nonzero effect. The *passive tier* is Conjecture 14.5.8. Definition 14.5.8e and Theorem 14.5.8f can construct and identify the support of a hard-core perspective Gibbs measure, and Corollary 14.5.8g bounds finite-window probability outside that support; none supplies the actualization, magnitude, reflexive-depth, source-separation, protocol-invariance, or no-signaling records required for a physical residue. Propositions 14.5.8b--d localize why SPAP diagonalization, cost-gradient bias, weak finite-resource limits, and window-local minimizers do not close the missing bridge.

*   **Paradox Residue:** (Appendix P, Definition P.16d.3.1; Theorem P.16d.3.2) Diagnostic tuple measuring failure of a proposed branch to descend to the finite operational quotient: naturality failure, PU-constraint violation, SPAP/RUD or role-collapse obstruction, certificate noncommutativity, or response-null surplus.

*   **Parameter Role Classes:** (Appendix P.14.1a) Paper-wide labels for what a quantity does inside a derivation or comparison: ExactThreshold, DiscreteMultiplicity, ThresholdData, BridgeNormalization, SchemeScale, CoarseGrainingScale, ReferenceConvention, PhenomenologicalKernel, SpectralMoment, and EmpiricalInput. These are not theorem statuses; they prevent exact thresholds, bridge normalizations, spectral moments, fit kernels, and observational inputs from being treated as the same kind of object.

*   **Pareto Signature (PU form):** (Section P.8.9a.11) The status-labeled family of concentration signatures generated by the Law of Prediction and, conditionally, by the Multiplicative PCE Noise Hypothesis: within-system disproportion $\rho(f;x^*)>f$, population Pareto tails with $\zeta=1+2k/\sigma^2$, fixed-shape Taylor scaling, and local small-noise inequality behavior.

*   **Pauli Reflection:** (Theorem G.8.4c.0c) The weak rank-2 block in the $3+2+1$ inactive decomposition is forced by the same active-rank source that produces the active Pauli algebra. Fiberwise, any local unitary isometry from the active rank-2 carrier to the weak rank-2 summand conjugates $\mathfrak{su}(2)_{\mathrm{act}}$ onto $\mathfrak{su}(2)_L$. This is a local frame theorem, not a canonical global bundle isomorphism.

*   **PCE Anomaly Matching:** (Appendix X.8d.5–X.8d.8) RG/PCE-flow constraint for retained global response symmetries: their anomaly class must be preserved under compression, after including Goldstone, topological, boundary, interface, and defect sectors. It is distinct from anomaly cancellation for redundancies.

*   **PCE-Attractor:** (Definition 15a, Appendix Z Section Z.6.3) The canonically selected PCE reference configuration characterized by maximal efficiency and symmetry. It determines the attractor coupling $u^*$ and supplies the reference background for the U(1) fine-structure calculation; stationarity and physical Gibbs/KMS equilibrium require independent channel, physical-time, and complete-passivity or detailed-balance certificates.



*   **PCE-Attractor Orbit:** (Theorem Z.6.3a) The orbit of the PCE-Attractor state $\rho_0 = \frac{1}{a}I_a \oplus 0_b$ under unitary conjugation is $\mathcal{O}_{\rho_0} \cong U(d_0)/(U(a) \times U(b)) \cong \text{Gr}(2,8)$, a compact Hermitian symmetric space of complex dimension $ab = 12$ and real dimension $2ab = 24 = M$. This identification provides an independent geometric derivation of the interface mode count.

*   **PCE Concentration Ratio:** (Definition P.8.9a.11.1) For equilibrium depth $x^*>0$ and effort fraction $f\in(0,1)$, $\rho(f;x^*)=[1-e^{-\kappa_{\mathrm{eff}}fx^*}]/[1-e^{-\kappa_{\mathrm{eff}}x^*}]$. The associated disproportion gap is $\Delta_{\rho}(f;x^*)=\rho(f;x^*)-f$.

*   **PCE-Descent RG Gate:** (Appendix X, Definition X.9.6c.2; Theorem X.9.6c.3; Corollary X.9.6c.4) Certificate machinery identifying RG beta flow with natural-gradient descent of a PCE compression potential exactly when the RG one-form $-\mathcal G_{ij}\beta^jd\theta^i$ is exact.

*   **PCE Experiment Value:** (Appendix P, Definition P.16d.10.1; Theorem P.16d.10.2) Expected reduction in admissible response-class entropy per positive experiment cost. Any maximal-value experiment is PCE-optimal for the stated single-step finite menu objective.

*   **PCE Potential ($V(x)$):** (Definition D.1) An effective potential function quantifying the net cost rate (operational, propagation, penalties) minus benefits for a given MPU network configuration $x$. The system's adaptation dynamics are modeled as a stochastic gradient flow seeking to minimize $V(x)$. Its reduction to the complexity coordinate is $V_{eff}(C)=\min_{x:\mathcal{C}(x)=C}V(x)$ under Theorem D.5a, with local Hessian $V''_{eff}=V_{CC}-V_{Cy}V_{yy}^{-1}V_{yC}$ on a smooth minimizing branch (Theorem D.5b).

*   **PCE (Principle of Compression Efficiency):** (Definition 15) The core optimization principle stating that adaptive systems evolve towards configurations that maximize predictive utility (Meaning Potential, related to high PP) while minimizing comprehensive resource costs (Signal Cost, including $R, R_I$, adaptation costs). Drives complexity adaptation and network self-organization via minimization of PCE Potential $V(x)$ (Appendix D). PCE is the operational content of the "resource-optimization imperatives" in PPI (Definition P.6.2); together they form a hierarchical principle where PPI asserts instantiation occurs under optimization and PCE specifies the optimization dynamics.

*   **$PE$ (Predictive Error):** Scalar discrepancy under a proper scoring rule (e.g., Brier or log‑loss). If $PE$ is a log‑loss it has units (nats/bits); otherwise we explicitly normalize it to be dimensionless.

*   **Perspectival Chain Reaction:** (Appendix P.15.5a) A finite cascade in which a compressed symbolic or contextual input changes a predictive agent's model and policy, causing downstream actions or communications that alter other perspectives or redirect ordinary external energy gradients. It is a control cascade, not a free-energy source.

*   **Perspectival Excess ($\Delta_\Sigma(S;E)$):** (Appendix P.15) The scalar excess of SPAP proximity over the SPAP-flat baseline, defined by
$$
\Delta_\Sigma(S;E)=\mu_S(E)-\frac{1}{\alpha_{SPAP}}.
$$
It vanishes on SPAP-flat patterns and is unbounded above on the independent-register amplification construction supplied by Theorem M.10.4.

*   **Perspectival Realism:** (Section 14.2) Philosophical stance suggested by the framework where reality has definite properties, but their description and actualization (via 'Evolve') are inherently relative to the interaction context or perspective $s \in \Sigma$.

*   **Perspectival Simulation Admissibility:** (Theorem M.10.11; Corollary O.4.3a) A simulation, reconstruction, archive, or forecast is admissible for a receiver only when every finite prefix actually integrated by that receiver stays at finite SPAP proximity. The obstruction is receiver-relative and content-relative, not a function of temporal direction or Shannon size.

*   **Perspectival State ($S_{(s)}(t)$):** (Definition 24, Appendix M) The typed pair $(\rho(t),s)$, where $\rho(t)$ is a density operator on $\mathcal H_0$ and $s\in\Sigma$ is the registered perspective index. Vector notation is reserved for the pure-state shorthand.

*   **Perspective Curvature:** (Appendix P, Definition P.16d.7.1; Theorem P.16d.7.2) Operational path-dependence of predictive transport across perspectives, measured by the retained holonomy defect $\operatorname{curv}_\gamma([X])=d_B(H_\gamma([X]),[X])$. Trivial holonomy is equivalent to path-independent global response gluing.

*   **Perspective-Indexed Semantic Closure:** (Appendix P.16b.2–P.16b.3 and P.16b.12) A family $R=(R_p)_{p\in P}$ closed under base kernels, reflexivity, same-perspective transitivity, and guarded cross-perspective adoption. The least closure exists by Theorem P.16b.2.4 and the proof system is sound and complete by Theorem P.16b.2.7. On the Borel-registered branch the global relation is analytic (Theorem P.16b.2.9), and its closure profiles carry symmetric and directed measure discrepancies (Theorem P.16b.12.8b). These relation discrepancies are distinct from the finite valuation metric $d_{\mathcal A}$. A monotone extension preserves a chosen self-indexed sector exactly when it admits the fixed-point invariant of Theorem P.16b.12.4d. Closure stages are derivation stages, not physical time, and semantic guards have no physical-import or heat meaning without separate certificates.

*   **Pfaffian Real-Orientation Certificate:** (Appendix K, Definition K.6.10a; Theorem K.6.10b) Certificate machinery orienting the real doubled quark Pfaffian line on a positive chamber. It forces $\arg(\det M_q)\equiv0\pmod{2\pi}$ unless the quark determinant gap closes or an explicit topological overhead is paid.

*   **Placebo Context:** (Appendix P.8.9a.7.5) An inert or semantically indirect intervention that changes a biological aggregate's physically instantiated context state and thereby shifts a reachable internal physiological channel. The represented proposition's truth value is not itself a causal primitive; the causal object is the encoded context state and its bounded coupling to the channel.


*   **POP (Prediction Optimization Problem):** (Axiom 1) The fundamental adaptive imperative for systems to optimize the predictive quality ($Q$) of their internal models concerning relevant aspects of their environment, under resource constraints.

*   **Population Configuration ($\mathcal P$):** (Appendix P.16a, Definition P.16a.1) A finite MPU-population datum $\mathcal P=(N,\mathcal T,\mathcal K,\rho)$ consisting of the MPU number, interaction topology, channel-capacity assignment, and finite-response retention class. For fixed $N$, the admissibility class $\mathcal A_N$ is the response-null quotient of admissible population configurations. For a surviving subset $S$, the notation $\mathcal P|_S$ denotes a nonempty induced subconfiguration or an admissibly relaxed finite-response quotient when the population model supplies one; otherwise the branch-restricted Appendix Z maps treat the outcome as off-branch.

*   **Population PCE Potential ($V_{\mathcal A_N}$):** (Appendix P.16a, Definition P.16a.4.1) The descended restriction of the existing PCE potential $V$ to the population-configuration admissibility class $\mathcal A_N$. When the infimum is attained or the relevant strict certificate supplies a selected minimizer, PCE selection on this class is compressed as $\delta V=0$ on $\mathcal A_N$, meaning global minimization over admissible finite-response population response classes rather than introduction of a new equation of motion. Without attainment or a strict certificate, the notation records minimizing-sequence/open status only.

*   **Minimal Awareness Interpretive Convention:** (Section 7.1.2) Interpretive postulate linking the operational $C_{op}$ cycle of an MPU to the most basic form of awareness.

*   **Summary of Postulate 2 (Causality Definition):** (Section 10.1.1; Theorem 39c) Operational causality requires exact independence of every pre-lightcone transcript from every independently selectable spacelike context. Zero-error incapacity is a weaker coding property, not the causality definition.

*   **Summary of Postulate 3 (Statistical FTL Influence):** (Section 10.3.1; Theorem 39b) Branch contract asserting that the CC mechanism acting on entangled systems may allow statistical influence across space-like separations, including a distinct nonlocal/state-mediated branch on which Bob's marginal may shift under late randomization. It is not a theorem-level consequence of the core substrate unless the corresponding protocol and empirical certificates close. The endpoint, zero-error, current, and ND-RID bounds control reliability and cost; by Theorem 39c a freely selectable late-randomized marginal shift is outside Postulate 2.

*   **Power-Law Learning Curve:** (Corollary 19.1, Equation 22f; Proposition 19.2a; Remark 19.2) The exact conditional image of Theorem 19 on a branch that independently satisfies $C-C_{op}=\eta_{\ell}\ln(N/N_0)$ is $\beta-\overline{PP}(N)=(\beta-\alpha)(N/N_0)^{-s}$ for $N\ge N_0$, with $s=\kappa_{\mathrm{eff}}\eta_{\ell}/\hat C_{\mathrm{target}}$. The value at $N=N_0$ is the formal analytic anchor $\overline{PP}(N_0)=\alpha$; physical $PP$ is its restriction to $N>N_0$. Proposition 19.2a tests exact finite-record compatibility using that formal anchor and off-boundary observations only; it proves neither interpolation nor the antecedents. Without an independent complexity measurement, only the composite exponent $s$ is identifiable.

*  **PP(t) (Predictive Performance):** (Definition 7, Equation 8) A normalized measure ($PP \in (0, 1]$) quantifying the effectiveness (quality $Q$) of a system’s predictions, inversely related to Prediction Error ($PE$). Viable systems operate with $\alpha < PP < \beta$.

*   **Prediction Coherence Boundary:** (Remark 3) The conceptual name for a class-relative quantitative boundary $\alpha_{SPAP}$ when a registered self-prediction protocol and certificate define it. Theorems 10–11 establish a diagonal nonexistence result, not a universal scalar. Theorem 14's resource bound applies only on its separately certified quantitative branch.

*   **Prediction Relativity:** (Remark 3) Structural analogy between the SPAP class-level diagonal obstruction and a task-relative quantitative boundary. When a registered task, score, admissible class, and certificate define $\alpha_{SPAP}<1$, and the Bernoulli reduction/cost certificate of Theorem 14 is accepted for a preregistered target $\alpha_{\mathrm{tgt}}$, the verification/update operation count obeys
$$
\Omega\!\left(\frac{\log(1/\delta_{SPAP})}{\delta_{SPAP}^2}\right),
\qquad \delta_{SPAP}:=\alpha_{SPAP}-\alpha_{\mathrm{tgt}}.
$$
SPAP alone supplies neither the scalar boundary nor this asymptotic cost law. Replacing $\alpha_{\mathrm{tgt}}$ by an observed $PP$ requires a registered identification that retains the same confidence condition. When the chosen complexity notion $C_P$ independently lower-bounds the certified verification/update operations, the same lower bound transfers to $C_P$.

*   **Prediction Status Class:** (Section 13, Definition 13.0f; Theorem 13.0g; Corollary 13.0h) Four-status classification of every numerical claim: derived-retrodictive, certificate-retrodictive, forward-locked, and prospective-confirmed or prospective-falsified. Only the fourth status counts as completed forward empirical evidence.

*   **Predictive Anomaly Descent/Inflow Principle:** (Appendix X, Theorem X.8d; Corollary X.8d.1) Transformations treated as predictive-frame redundancies must have vanishing total anomaly class so that the predictive functional descends to the quotient of physically equivalent descriptions. Bulk anomaly classes may be canceled by boundary, horizon, or interface classes. A nonzero anomaly for a global current not treated as a redundancy is instead a physical update channel.

*   **Predictive Current Certificate:** (Definition 10.2c; Theorem 39b) A finite transport representation of statistical influence in which Bob's marginal shift is the Bob projection of a retained predictive-current divergence. It is admissible only with the no-zero-error common-support gate, and a thermodynamic precision law is theorem-level only when supplied by a finite Markov/KMS certificate.

*   **Predictive Curvature Principle:** (Theorem 47, Theorem G.4b, Corollary G.4b.1, Corollary X.8b) On the regular product-bundle branch, local predictive frames form the joint spin-internal bundle $\mathcal W=S\otimes E$ with predictive connection $D^{\mathrm{pred}}=\nabla^{\mathrm{spin}}\otimes 1+1\otimes\nabla^{\mathrm{int}}$. Its curvature
$$
\mathcal F^{\mathrm{pred}}=R(\Omega)\otimes 1+1\otimes F(A^{\mathrm{int}})
$$
is the loop-closure obstruction for predictive-frame transport.

*   **Predictive Failure Taxonomy:** (Appendix P, Definition P.16d.19.1; Theorem P.16d.19.2) Classification of knowledge failure by the broken finite-presentation component: context, response expectation, update, verification, cross-perspective transfer, quotient selection, surplus, status, or model capacity.

*   **Predictive Fisher Metric:** (Corollary 23c.1; Corollary 45b) The positive semidefinite tensor $h=\frac14F^Q$ on a retained MPU response-state chart. On pure-state branches it equals the Fubini-Study metric. After quotienting response-null directions it becomes the local identifiable distinguishability metric. It coincides with the positive operational propagation metric only on the finite certificate branch where the same retained protocol family supplies both Fisher distinguishability and the Cheeger propagation quadratic form.

*   **Predictive Infrared Admissibility Gate:** (Appendix X.8j.4) PPI rule for massless long-range sectors: a scattering quantity is observable at detector resolution $\lambda$ only if it descends to the quotient by unresolved soft-ledger refinements below $\lambda$. Inclusive and dressed observables are sufficient representatives on their regular branches.

*   **Predictive Island:** (Appendix K.3b–K.3d) A disconnected interior or horizon-adjacent Markov-blanket component admitted into exterior radiation reconstruction when the reduction in predictive entropy exceeds its boundary channel-capacity cost. It is a finite channel-capacity object; geometric area appears only in the regular thermodynamic representation.

*   **Predictive Kernel / Local Dressing:** (Appendix P.16b.15a) At fixed population, budget, protocols, decoders, and comparison branch, the retained cultural-function unit is the collective response-quotient point $\operatorname{Role}_{\mathcal S}(K_x)$. A local dressing $D_\Gamma(K_x)$ is a transmissible code whose decoded role is tolerance-controlled relative to that point. This semantic response role is not one of the system-level capabilities $b_m,b_p,b_v$, is not $D_{cyc}$, and does not determine a physical carrier or mechanism.

*   **Predictive Large-Deviation Instanton:** (Appendix Q.0.1a–Q.0.1c) Minimum-action rare transition on a finite ND-RID/PCE branch satisfying a path-space large-deviation principle. Its suppression exponent is the infimum of the dimensionless action $I_{\mathrm{PU}}=\mathcal S_{\mathrm{PU}}/\hbar$ over retained transition paths.

*   **Predictive Momentum Ledger:** (Corollary Y.1.1; Corollary O.3a.4) The CP-odd, forward-oriented baryon transport sum $\mathcal P_B(\Omega)=\sum_e\chi(e)q_B(e)w(e)$. It vanishes pairwise on CP-symmetric equilibrium branches and becomes nonzero only when the Appendix Y anomaly, CP, out-of-equilibrium, sphaleron, washout, and transport certificates supply unequal paired weights.

*   **Predictive Nuclearity Species Bound:** (Appendix F.10.4f) Local phase-space density gate for regular AQFT branches. The energy-damped map $\Theta_{\beta,O}(A)=e^{-\beta H}A\Omega_*$ must have nuclear norm bounded by finite PU channel capacity, preventing an inadmissible density of local species or excitations.

*   **Predictive Price Principle (Constraint-Coupling Duality):** (Appendix X, Theorem X.8c; Appendix Z, Corollary Z.8.2a) On regular constrained PCE branches, the coefficient of an active physical admissibility constraint is its KKT shadow price after the branch normalization map. In canonical stiffness conventions $g_A^{-2}=\mathcal N_A\lambda_A$; in rate-coordinate branches, the active boundary coordinate $u_A^*$ and Ward/interface normalization give $\alpha_A=u_A^*/(4\pi\kappa_A)$, while the multiplier gives the marginal value of relaxing the capacity bound.

*   **Predictive-Recovery MacWilliams Branch:** (Appendix Z, Definition Z.13b.0; Theorem Z.13b.0a) Rate-selection branch on which prediction payload and recovery syndrome are assigned dual dimensions and the penalty $V_{\mathrm{dual}}(C)=\lambda_{\mathrm{dual}}(\dim C-\dim C^\perp)^2$ is minimized. At $n=24$, the admitted zero-penalty witness forces $k=12$ for every rate minimizer. This is a dimension equality, not code self-duality. Selection of the extended Golay code additionally requires an existence/selection certificate attaining $d=8$; Theorem U.1 then classifies the resulting binary linear $[24,12,8]$ code up to coordinate permutation. The Leech and cosmology steps retain their independent gluing and physical certificates.

*   **Predictive Renormalization Group (PRG):** (Appendix P, Definition P.16d.1.1; Theorem P.16d.1.2) Functorial flow between finite predictive quotients $Q_{B_1}\to Q_{B_2}$ under compatible coarsening of protocol, resolution, tolerance, retained maps, or cost budget. A distinction is physically retained at a budget only if it survives the corresponding finite-response quotient, changes a retained map, supplies a certificate role, or lowers the descended PPI/PCE cost.

*   **Predictive Resonance Spectrum:** (Appendix X.9.6d) On a branch with a supplied faithful stationary PCE/KMS state and primitive active dissipative quotient, the pole spectrum of $(z-\mathcal L_{\mathrm{PCE}})^{-1}$ contains a simple stationary zero mode and the decay/mixing resonances. Protected Hodge modes and transport, finite-OTOC, or memory poles are included when the corresponding observable is expressible as a Laplace transform of an active MPU correlation.

*   **Predictive Thermodynamic Uncertainty Bound:** (Appendix D.8.7e–D.8.7g) Let $(X_t)_{t\ge0}$ be a finite irreducible continuous-time Markov jump process initialized in its stationary law $\pi$, with finite rates satisfying $k(x,y)>0$ if and only if $k(y,x)>0$. For an antisymmetric integrated jump current $J_T$ with $\langle J_T\rangle\ne0$ and total stationary entropy production $\Sigma_T$, the finite-time bound is
$$
\frac{\operatorname{Var}(J_T)}{\langle J_T\rangle^2}\Sigma_T\ge2.
$$
A Blackwell-PCE record current inherits this conclusion only after its output law independently satisfies these Markov, stationarity, support, and current hypotheses.

*   **Primordial Complexity Product Lock:** (Appendix U, Corollaries U.65a–U.65b; Section 13.10.6) On the predictive-recovery MacWilliams Golay primordial branch and Starobinsky effective branch,
$$
A_s r=\frac{A_Qe^{-22}}{4\pi^2},
$$
where $22=\dim_{\mathbb R}(\mathbb{CP}^{11})$.

*   **Principle of Physical Instantiation (PPI):** (Appendix P) Any abstract computational/logical requirement realized by a finite physical system incurs irreducible thermodynamic costs and hence is subject to optimization (PCE). Physical laws reflect resource-efficient embodiments of these instantiated logical structures.

*   **Prohibition Set ($\mathcal{L}^{\neg}_S$):** (Definition P.16.1) The set of candidate post-update self-model configurations $\theta'\in\Theta_S$ that the self-modeling map $\Pi_S^{(PP)}$ cannot realize at any admissible $PP<\alpha_{SPAP}$ (Definition M.10.3); equivalently, realization forces the SPAP proximity $\mu_S$ to diverge. Theorem M.10.4 supplies explicit nonempty examples on its independent-register amplification branch. On a nonempty branch admitting the approach sequences of Lemma P.16.1, the cost potential $U_S$ diverges on $\mathcal L_S^{\neg}$ without making that set a positive degree of freedom. A physical source-energy or stress-energy footprint requires additional data: a typed integration task and SPAP-flat baseline; registered reset, temperature, and time ledgers; a finite maintenance window; the retained stress-energy convention; and the response/coupling branch of Proposition N.8.1. The *coupled* prohibition set $\mathcal{L}^{\neg}_{\{S_i\}}$ (Definition P.16.2) is the subset of $\prod_i\Theta_{S_i}$ in which at least one factor lies in its individual prohibition set, intersected with $\operatorname{supp}(G_{\mathrm{persp}})$.

*   **Proof-Cost Datum:** (Appendix P, Definition P.16d.12.1; Theorem P.16d.12.2) Finite physical cost ledger for producing and verifying mathematical proofs or certificates. Lower-cost certificates dominate higher-cost certificates with the same verification response unless the latter supply an independent certificate role.

*   **Proper-Acceleration Entropy Drag:** (Appendix N, Corollary N.12b) UCT branch on which a system undergoing proper acceleration has excess predictive-refresh power $P_{\mathrm{UCT}}=q_{\mathrm{act}}cm_S|a|/(2\pi)$ and dimensionless observable $\mathcal Q_a=2\pi P_{\mathrm{excess}}/(c m_S|a|)=q_{\mathrm{act}}$.

*   **Property R (Computational Richness):** (Definition 10, Appendix A) The finite diagonal-closure capabilities needed for the SPAP and RUD arguments: representation, simulation/reasoning, predicate evaluation, logical memory, retained finite-program composition, and Boolean post-processing. Appendix A.0.2 derives an abstract finite-program realization from its declared logical-structural assumptions. A physical MPU-network realization is conditional on the refresh, reduced-cost, Dominant Cost Convexity, and robustness certificates of Theorem A.0.2; an accepted QEC or Golay-QEC certificate; and the working-memory, protected-gate, code-overhead, and circuit-execution resources of Theorem A.0.6. Corollary A.4.1b supplies recursion-theorem presentations for admissible finite verifier transformers but does not replace operational alignment or establish $K_0=3$.

*   **Proposition 4 (Complex Hilbert Representation):** (Section 7.2) A unital complex $C^*$-algebra with a positive normalized state has a cyclic complex Hilbert representation by GNS. The bound $d_0\ge8$ additionally requires Theorem 23's full-context distinguishability branch.

*   **Schrödinger Dynamics:** (Section 8, Theorem 8.5) Continuous time-translation-symmetric transition-probability-preserving ray dynamics has a strongly continuous unitary implementation and hence a self-adjoint generator.

*   **Summary of Proposition 6 (Interpretation of Superposition):** (Section 8.2) On the complex Hilbert representation, vector expansions are interpreted as available predictive branches; the algebraic expansion itself does not derive physical actualization.


*   **Summary of Proposition 7 (Born Rule):** (Section 8.3, Theorem G.1.7) Normalized noncontextual weights additive on finite effect decompositions have trace form on the complex Hilbert branch of dimension at least three.

*   **Propositions 8–11 (Conditional QM Results):** (Sections 8.4–8.7) The Robertson inequality assumes Hermitian operators; measurement assumes a supplied instrument and registered outcome branch; entanglement assumes tensor-product/local-CPTP composition; Schrödinger evolution assumes the continuous ray-dynamics package.

*   **$|\psi(t)\rangle$:** A representative vector used on the pure-state branch $\rho(t)=|\psi(t)\rangle\langle\psi(t)|$; the general quantum component of Definition 24 is the density operator $\rho(t)$.

*   **PU One-Particle Sector Label:** (Definition F.5b.1; Proposition F.5b.2) On a regular AQFT/DHR branch, a retained one-particle sector is labeled by $(\mathcal O_p,\lambda_{\mathrm{Wig}},\rho_{\mathrm{int}},\chi)$ on the multiplicity-free retained branch, with enriched labels $(\mathcal O_p,\lambda_{\mathrm{Wig}},\rho_{\mathrm{int}},\chi,\iota_{\mathrm{mult}})$ when multiplicities arise. Here $\mathcal O_p$ is a positive-energy Poincaré orbit, $\lambda_{\mathrm{Wig}}$ is the Wigner little-group label, $\rho_{\mathrm{int}}$ is a simple object of the retained DHR category, $\chi$ is Weyl chirality data, and $\iota_{\mathrm{mult}}$ is the retained-multiplicity index. Antiparticles use DHR conjugate objects. CPT closure requires the modular or Wightman/Jost gate, not a three-label shortcut. Continuous-spin Wigner reps are excluded by branch retention rather than by representation theory.

*   **PU One-Way-Function Bridge Certificate $\mathfrak C_{\mathrm{OWF}}$:** (Definition O.5.2f; Theorem O.5.2g; Corollary O.5.2g.1; Theorem O.5.3c; Remark O.5.3i) Finite proof-carrying record for one fixed uniform polynomial-time function family, the locked uniform input ensemble, any-preimage success predicate, full uniform-$\mathsf{PPT}$ negligible-inversion quantifiers, computational-to-PU simulation bridge, proof-system soundness warrant, and non-circular dependency ledger. Unconditional acceptance implies $\mathsf{P}\ne\mathsf{NP}$ through this route. The scoped relativization barrier excludes only routes whose premises are oracle-stable and whose steps are separately certified oracle-natural; current value: $\varnothing_{\mathrm{cert}}$.

*   **Oracle-Natural Bridge:** (Definition O.5.3a; Theorem O.5.3c) An admissible bridge carrying the additional proof that its encoders, decoders, solver transformation, distribution transport, overhead, and success-loss bounds commute with adjoining every common oracle. Admissibility alone does not imply oracle-naturality.

*   **Direct Separation Certificate $\mathfrak C_{\mathrm{sep}}$:** (Definition O.5.3e; Proposition O.5.3e.1) Sufficient proof-carrying format for a direct $\mathsf{P}\ne\mathsf{NP}$ route: a checked proof in a named theory, an accepted standard-model soundness warrant for the used fragment, and no unresolved dependency. Current value: $\varnothing_{\mathrm{cert}}$.

*   **Physical Hardness Conditional Branch:** (Definition O.5.3h; Theorem O.5.3h.1) Marked branch on which $[\mathrm{PHP}]_{f_{\mathrm{PHP}}}$ and a quantitative PPT-to-PU simulation certificate imply that $f_{\mathrm{PHP}}$ is one-way and hence $\mathsf{P}\ne\mathsf{NP}$. The postulate and any unproved simulation certificate remain explicit ledger assumptions; the branch is not an unconditional theorem. Current simulation-certificate value: $\mathfrak C_{\mathrm{PPT}\to\mathrm{PU}}=\varnothing_{\mathrm{cert}}$.

*   **Weak $\mathsf{AC}^0$ Inversion Obstruction:** (Proposition O.5.3g; Corollary O.5.3g.1) Prefix-difference bijection whose exact inverse exposes parity in its final coordinate. The displayed reduction bounds full-vector inversion success by $1/2$ plus the parity-correlation term; this does not by itself discharge negligible inversion or its negation.

*   **PU Spectral-Action Transfer Ledger:** (Appendix X, Definition X.9.6h.4; Theorem X.9.6h.5; Appendix T, Corollary T.17a.0b; Theorem T.78.14) Certificate machinery making gauge kinetic coefficients, Higgs kinetic, $\mu_H^2$, $\lambda_H$, and electroweak threshold finite parts deterministic heat-coefficient projections of a certified first-order PU Dirac operator only after the cutoff, projections, parity/grading data, heat coefficients, finite-part scale, subtraction order, tail bounds, normalization map, and interval ledger are accepted before comparison; otherwise the current spectral-action Higgs and threshold entries are $\varnothing_{\mathrm{cert}}$.

*   **$Q$ (Predictive Quality):** (Section 2.1) General measure of the goodness or utility of a system's predictions relative to its POP goals (e.g., reduced uncertainty, increased accuracy, enhanced operational success). Related to Predictive Performance PP.

*   **QFI Mode Count ($M$):** (Appendix Z, Theorem Z.5, Proposition Z.1) The number of independent information-sensitivity modes in the PCE-Attractor state: $M = 2ab = 2 \times 2 \times 6 = 24$, with per-mode sensitivity $\lambda = 1$. Arises from off-diagonal QFI contributions connecting active ($a = 2$) and inactive ($b = 6$) subspaces. Fixed on the minimal branch and audited by the dependency-separated compatibility ledger of Theorem Z.12; Niemeier classification is a separate record.

 Geometrically, $M = \dim_{\mathbb{R}}(\text{Gr}(2,8))$ equals the real dimension of the PCE-Attractor orbit, providing independent confirmation via differential geometry. It is an internal interface-mode count, not by itself a theorem-level graph degree asserting twenty-four distinct neighboring MPUs.

*   **QFI (Quantum Fisher Information):** (Appendix Z, Section Z.5) For a differentiable one-parameter state family $\rho(\theta)$, the symmetric-logarithmic-derivative quantum Fisher information $F_Q(\theta)$ upper-bounds the classical Fisher information obtainable from any measurement. If $n$ independent copies are measured and $\hat\theta$ is locally unbiased at $\theta$ on a regular branch with $0<F_Q(\theta)<\infty$, then $\operatorname{Var}_\theta(\hat\theta)\ge1/[nF_Q(\theta)]$. In the PU framework, the registered QFI tensor supplies the MPU interface information-sensitivity modes on the stated branch.

*   **$R(C)$:** (Definition 3, Equation 4) Physical Operational Cost function. Power-like rate of *physical* resource consumption required to maintain and operate structures/processes of complexity $C$. $R(C) = R(C_{op}) + r_p (C-C_{op})^{\gamma_p}$ for $C \ge C_{op}$, with $\gamma_p > 1$.

*   **$R_I(C)$:** (Definition 3, Equation 5) Reflexive-Information Cost function. *Informational* overhead rate associated with self-referential verification/processing. $R_I(C) = (r_I/\ln 2) \ln(C/K_0)$ for $C > K_0$.

*   **Ramanujan-Type Mathematical Event:** (Appendix P.2.6.3e) A mathematical-cognition event in which an assertion $E$ is retained by the full predictor through a positive finite verification register, $\Delta V_S(E\mid\mathcal V_S)<0$, while the deliberative subprocess lacks the current budget to serialize the public proof, $C_{\mathrm{seq}}(\pi_{\mathrm{seq}}(E))>B_{\mathcal D_S}$.

*   **Rank-Pareto Zipf Bridge:** (Theorem P.8.9a.12.2) A Pareto survival tail with exponent $\zeta$ gives the deterministic rank-size curve $x_r=x_0(N/r)^{1/\zeta}$.

*   **Reflexive Burden Spectrum:** (Appendix P, Definition P.16d.11.1; Theorem P.16d.11.2) Retained energy, latency, noise, entropy, and complexity differences between matched self-referential and non-self-referential tasks. It lower-bounds self-model burden when a matching certificate isolates the additional self-context channel.

*   **Reflexive Exhaust Ledger:** (Theorem J.4a; Proposition U.2b) The finite entropy-action accounting term $\mathcal X_{\mathrm{ref}}=\sum_e[H(P_e\mid R_e)+\varepsilon_{\mathrm{diss}}(e)]$ for completed nontrivial SPAP/Evolve updates. In the vacuum sector it can enter only as a finite action factor inside an accepted Appendix U certificate.

*   **Reflexive Opacity:** (Appendix P, Definition P.16d.6.1; Theorem P.16d.6.2) The finite-cost limit on complete transparency of a Property-R self-modeling system. Nonzero self-context information measured in nats obeys $\Delta I(\Delta S_{\min}/k_B)\ge\kappa_r$ on the reflexivity-constraint branch.

*   **Reflexivity Constraint:** (Theorem 33, Equation 48) Fundamental trade-off $\Delta I \cdot (\Delta S_{min}/k_B) \ge \kappa_r > 0$ between relevant information gain $\Delta I$ in an 'Evolve' interaction and the minimum necessary dimensionless state disturbance (entropy production) $\Delta S_{min}/k_B$. Requires the theorem's registered information-disturbance branch; it does not follow from the structural value $\varepsilon_0=\ln2$ alone.

*   **Register-Coherent Active Admissibility:** (Appendix A.5.6a, Definition A.5.6a.7) A proof-admissibility predicate satisfying
    $$
    \operatorname{Adm}^{\mathrm{act}}_B(t,p,e,y)
    \Longrightarrow
    P_B(t,e)=y,
    $$
    for $y\in\{0,1\}$. It ensures that active evidence supports the Boolean label actually issued by the current register, and excludes active Boolean proof admission when the register value is $\bot$.

*   **Regularity:** (Definition C.3) Geometric Regularity of the MPU network, encompassing uniform volume growth and bounded curvature. Necessary condition for viability (Theorem 43).

*   **Residual-Status Ledger:** (Appendix P, Convention P.14.1i) Companion table to the master branch-exclusion ledger, listing the items whose outputs can vary while preserving the prior theorem stack. Records each residual gate's correct status (axiom, postulate, hypothesis, identification, assumption, branch data, or open bridge condition) and the reason it cannot be promoted to theorem status by prose alone.

*   **RID (Reflexive Interaction Dynamics):** (Definition 6) Formalism characterizing interactions where the system's subsequent state depends intrinsically on the outcome of the interaction itself. Includes deterministic (D-RID) and non-deterministic (ND-RID) versions.

*   **RU (Reflexive Undecidability):** (**Theorem 12, Theorems A.2.3, A.2.4**) The fixed terminal-reachability property $\mathsf{TERM}$ is not uniformly decidable on the effectively coded pointed RID simulation class. The probabilistic extension requires a computable finite runtime bound, computable complete finite support lists with rational probabilities, and a fixed rational advantage $0<\epsilon<1/2$.

*   **$s_{bg}$ (Cosmological Sparsity Variable):** (Definition K.9.1b) The homogeneous FLRW relaxation driver
$$

s_{bg}(t)=\frac{\rho_\Lambda}{\rho_b(t)+\frac{4}{3}\rho_r(t)},
$$
with $\rho_\Lambda=\Lambda/(8\pi G_0)$. It measures how close the non-vacuum background is to the de Sitter floor.

*   **$S_{(s)}(t)$:** (Definition 24) The Perspectival State $(\rho(t),s)$; for $\rho=|\psi\rangle\langle\psi|$, $(|\psi\rangle,s)$ is an explicit pure-state shorthand.

*   **Scalar-Channel Hyperon Spin Filter:** (Appendix Z, Corollary Z.8k.1a) Collider-level scalar $0^{++}$ branch on which the local $\Lambda\bar\Lambda$ spin-correlation slope is nonzero in the short-range charge-conjugate channel while long-range, same-sign, and spin-zero controls vanish.

*   **$\Sigma$ (Sigma - Perspective Space):** (Definition 25, Theorem 25, Appendix M) Mathematical space of complete interaction-context flags for an MPU, identified on the stated branch with $\Sigma\cong U(d_0)/U(1)^{d_0}$ of complex dimension $d_0(d_0-1)/2=28$ and equipped with the metric $d_\Sigma$ of Equation 42. For an active/inactive split $d_0=a+b$, the PCE-Attractor orbit is the partial-flag space $\operatorname{Gr}(a,d_0)=U(d_0)/(U(a)\times U(b))$ of complex dimension $ab=12$. The canonical relation is the projection $\pi_a:\Sigma\to\operatorname{Gr}(a,d_0)$ that sends a complete flag to its active $a$-plane; identifying the Grassmannian with a submanifold of $\Sigma$ would require additional section data.

*   **Size- and Data-Coordinate Power Law (PU form):** (Corollary 19.2, Equation 22h; Proposition 19.2a; Remark 19.2) The exact conditional image obtained only when a registered aggregate branch satisfies Theorem 19's multiplicative residual-composition hypothesis and an independently registered logarithmic coordinate-to-complexity relation. Calling a coordinate neural, technological, biological, or cultural requires a separate empirical bridge. Once $\alpha$, $\beta$, and the baseline coordinate are fixed, the curve identifies only $\kappa_{\mathrm{eff}}\eta_\bullet/\hat C_{\mathrm{target}}$, not its factors separately; the common curve form does not identify a common mechanism.

*   **Source Energy ($\mathcal{E}_{\mathrm{src}}$):** (Appendix P.15) The excess reflexive thermodynamic dissipation generated when a predictive system processes patterns whose SPAP proximity exceeds the SPAP-flat baseline. Over an interval $\Delta t$,
$$
\mathcal{E}_{\mathrm{src}}(S,\Delta t)=\sum_j Q_{\mathrm{src}}(S,E_j),
$$
where $Q_{\mathrm{src}}$ is the excess heat from the SPAP-dependent part of reflexive integration and vanishes on SPAP-flat inputs. It is distinct from the baseline operational cost of the predictive loop. Source Energy is not stored in the message; it is generated on the receiver side when the receiver integrates the pattern into its own self-model above baseline.

*   **Space of Becoming:** (Definition 8) The open interval $(\alpha, \beta)$, representing the necessary operational range for Predictive Performance $PP(t)$ for viable, adaptive MPU functioning (Axiom 3).

*   **SPAP (Self-Referential Paradox of Accurate Prediction):** (**Section 4.2, Theorems 10, 11, A.1.1, A.1.3**) Diagonal theorems excluding guaranteed perfect self-prediction on the stated retained finite-program classes with their representation, simulation, predicate-evaluation, logical-memory, Boolean-post-processing, and self-coding closure conditions. They establish class-level Logical Indeterminacy. A numerical performance boundary $\alpha_{SPAP}$ requires a separate class-relative protocol and quantitative certificate.

*   **SPAP Three-Register Inner Symmetry:** (Section 7, Theorem 23a; Corollary 23a; Theorem G.10.3; Remark 23a.1e) Label-preserving projective inner symmetry of the fixed Memory-Prediction-Interface factor frame is $(\mathrm{PSU}(2))^3$. If the branch treats the factor frame as unordered and permits register relabeling, the frame normalizer is $(\mathrm{PSU}(2))^3\rtimes S_3$. Preserving the full $M_8(\mathbb C)$ algebra alone is not enough; the three-factor frame is the relevant datum.

*   **Spectral Branch Package:** (Theorems T.78.2, T.78.5, and T.78.14) The additional data needed to turn the structurally formulated flag-lift threshold problem on $\widetilde X$ into a completed numerical block-sum computation: metric normalization, connection, structural parameters, sector/parity block data, heat/zeta finite-part data, and explicit tail certificates. The current canonical ledger does not supply such a package PU-internally; Theorem T.78.14 further classifies the current RHG, torsion, spectral-action, and equivalent electroweak source class as negatively closed for $\Delta_i$, $Z_i$, $\mu_H^2$, $\lambda_H$, and gauge/Higgs finite-part intervals. A positive numerical computation requires an appended spectral branch extension fixed before validation comparison and accepted without retuning finite-part convention, projector, tail bound, grading, normalization, or master zeta-index ledger after any dependent row is fixed.

*   **SpectralMoment:** (Appendix P.14.1a; Appendix V.3.11) Parameter role for a coefficient or matrix entry obtained as $s_B\langle\sigma_B\rangle_{\omega_*}$ from an accepted finite spectral calibration datum. A SpectralMoment is not a validation target, reference convention, phenomenological kernel, or empirical input.

*   **Spectrum-Broadcast PPI Objectivity:** (Appendix M.6.11d–M.6.11f) Multi-fragment objectivity criterion: a PCE-minimal record becomes public when disjoint environmental fragments independently recover the same classical statistic and are conditionally independent, equivalently when the compressed state has spectrum-broadcast form.

*   **Statistical FTL Influence:** (Postulate 3, Section 10.3) Hypothesized dependence of Bob's pre-lightcone marginal on Alice's independently selectable spacelike context. Theorems 39a and 42 can exclude finite-window zero-error decoding, but Theorem 39c shows that any nonzero dependence has positive Shannon capacity and lies outside the sealed causal branch. It may have positive finite-error statistical information, but on the regular finite-window branch (Definition 10.2a) it lacks deterministic or zero-error FTL capacity (Theorems 39a and 42).

*   **Strict-Certificate Registry:** (Appendix P, Definition P.14.1m; Algorithm P.14.1m.0; Theorems P.14.1m.1 and P.14.1m.2; Corollary P.14.1m.3; Remarks P.14.1m.4 and P.14.1m.5) Schema for a machine-checkable finite-record realization of the Global Strict-Certificate Ledger. The tuple $\mathfrak G_{\mathrm{PU}}=(\mathcal V,\mathcal E,\kappa_{\mathcal V},\sigma,\rho,\beta,\theta,\gamma,\delta,\zeta)$ records vertices, dependency edges, vertex-kind assignments, status labels, residual lists, residual-control records, closure gates, overlap audits, forward-lock indicators, and spectral-calibration/master-zeta records. Conditions (C11) and (C18) audit and preserve the total kind map. Theorem P.14.1m.2 gives canonical encoding and round trip only for a tuple whose semantic checks (C1)--(C17) have independently passed and whose populated codec certificate passes the finite field-, identifier-, operator-, and type-table audit. The displayed schema is neither a populated registry instance nor a populated codec certificate. Machine-audited status requires a versioned populated tuple, exact input hash, versioned executable verifiers or proof kernel, termination and soundness certificates for the invoked predicates, and a recorded passing (C1)--(C18) execution. None is supplied, so the paper's statuses remain manuscript-ledger assignments. The compression theorem applies only to an accepted populated finite snapshot.

*   **Strict PPI/PCE Certificate:** (Definition D.8.9a) A finite or compact sector record $\mathfrak C_S=(Q_S,\sim_S,\mathcal R_S,V_S,q_S^*,g_S,\Pi_S)$ containing the admissible finite-response candidates, operational quotient, response presheaves, PCE functional, selected minimizer, strict gap, and overlap maps. When present, Theorem D.8.9b forces the sector value modulo response equivalence.

*   **Structural-Invariant Projection on a Strict Branch:** (Appendix D, Definition D.8.9a; Theorem D.8.9b; Remark D.8.9d.1) On a branch with accepted strict PPI/PCE certificate, an internal finite invariant well-defined on response-equivalence classes has a unique value at the selected class. The remark does not collapse the recurrent-ledger source roles of Theorem R.3.5e.3.

*   **Structural Mass-Cost Ledger:** (Definition T.14.2a; Proposition T.14.2b) On certified mass branches, multiplicative root, holonomy, sector, generation, and running factors reorganize as the additive cost $\Omega_{\mathrm{mass}}=-\log(m/v)$. On branches with sector-signature-only running and partitioned certified factors, same-signature mass-cost differences cancel the running, universal, and sector contributions, isolating the generation cost.

*   **T1/T2/T3 Uncertainty Protocol:** (Appendix P.14.1c; Appendix T.25.5.3) Paper-wide decomposition of numerical uncertainty. T1 records internal truncation and approximation inside a fixed branch; T2 records matching, threshold, branch, bridge, regularization, and convention dependence; T3 records empirical extraction, observable mapping, target-tuple retention, phenomenological-kernel fitting, and discrete identification ambiguity.

*   **$T_{\mu\nu}^{(MPU)}$ (MPU Stress-Energy Tensor):** (Definition B.8, Equation B.15, Appendix B) A macroscopic symmetric continuum source available on the Appendix-B certificate branch: admissible bounded-variation coarse-graining, paired or unique continuum limits, momentum-flux and Belinfante conservation identities, variational first-variation consistency, local-equilibrium continuity, and global horizon-flux consistency with convergent quadrature. On the matching Section-12 horizon branch, the same certified tensor appears in Equation 76.

*   **$\mathcal{T}$ (Simulation Horizon/Steps):** (Used in Theorems 14, B.2) Dimensionless number of steps or cycles over which a SPAP-limited prediction task is performed or a simulation is run.

*   **Taylor Scale Law:** (Theorem P.8.9a.12.9) Let $Y$ be a nonnegative random variable whose law is independent of $a$, with $0<\mathbb E[Y]<\infty$ and $\operatorname{Var}(Y)<\infty$, and let $X_a=aY$ for $a>0$. Then $\operatorname{Var}(X_a)=[\operatorname{Var}(Y)/\mathbb E[Y]^2]\mathbb E[X_a]^2$, so the scale-family Taylor exponent is exactly $2$.

*   **Theorem 23 (Minimal Hilbert Space Dimension):** (Section 7.2.2) Theorem 15's realization class satisfying (O1)–(O3) and (FC) has $N_{\mathrm{vis}}^{\min}=2^{K_0}=8$. When these contexts are represented by mutually perfectly distinguishable states on a complex Hilbert carrier, $d_0\ge8$. Selection of $d_0=8$ requires the additional minimal-branch hypotheses of Theorem Z.2.

*   **Theorem 28a (Conditional Born-Rule Representation):** (Section 7.3.4) If the retained state space is a complex Hilbert space of dimension at least three and the effect weights are normalized, noncontextual, and additive on every finite effect decomposition of the identity, then a unique density operator represents them by $\omega(E)=\operatorname{tr}(\rho E)$. SPAP motivates a prediction limitation but does not derive the stochastic law or the selector hypotheses.

*   **Theorem 29 (Physical Interpretation of Internal Hamiltonian):** (Section 7.3.3) The expectation $\langle\hat H\rangle$ gives the baseline operational-energy interpretation, and the spectrum gives a characteristic timescale after a positive energy scale and task are fixed. Corollary 29.1 gives an orthogonalization bound; no universal positive duration for arbitrary updates follows.

*   **Theorem 31 (Conditional Physical Reset Bound):** (Section 7.4.4; Definition 28) A registered classical reset obeys $\varepsilon_{\mathrm{reset}}=H_q(P\mid R)+\varepsilon_{\mathrm{diss}}$ with $\varepsilon_{\mathrm{diss}}\ge0$ and all retained unchanged side information included in $R$. Proposition 5 and Definition 28 separately assign the declared binary reset-support alphabet the structural value $\varepsilon_0=\ln2$. No universal positive physical floor follows from alphabet size alone.

*   **Summary of Theorem 43 (Conditional Selection of Geometric Regularity):** (Section 11.3.1–11.3.2, Appendix C, Appendix D) On the Appendix-C viability/cost branches and effective Theorem D.3's strict regular-comparator branch, no irregular configuration is a global PCE minimizer. Low-noise concentration adds Theorem D.5's detailed-balance hypotheses. The operational manifold, AQFT, Lorentzian, and gravity constructions require their separate continuum and response certificates.

*   **Theorem 48a.0 (Certificate-Conditioned Local Thermodynamic Equilibrium at Causal Horizons):** (Section 12.1.2) Establishes local horizon equilibrium only on a branch carrying the independent physical-equilibrium and modular-boost certificates used by Theorem 48a.



*   **Thermodynamic Dressing:** (Appendix P.6.1) A tuple
    $$
    X^\Theta=(X,\Pi_X,R_X,V_X,C_X,\tau_X)
    $$
    converting a bare mathematical object or aspect $X$ into a finite-cost physicalization candidate with protocol, record, verifier, cost, and update-ordering data.

*   **Thomson-Ward Spectral-Weight Certificate:** (Appendix Z, Definition Z.14d; Theorem Z.14e) Certificate machinery closing the Ward-map unit Jacobian $c_{\mathrm W}=1$ on a branch where the operationally measured current-current response on the retained Paley-Wiener bandwidth saturates the active-charge sum rule $\int_0^{\Omega_{\mathrm{PW}}}(d\omega/\pi)w_T(\omega)\operatorname{Im}\chi_{JJ}^{\mathrm{op}}(\omega)=\mathcal Q_{\mathrm{PU}}=2$ via the standard $f$-sum/TRK derivation, while the QFI-canonical Ward map gives $c_{\mathrm W}^{-1}\mathcal Q_{\mathrm{PU}}$ for the same integral. Comparison forces $c_{\mathrm W}=1$.

*   **Toeplitz-Kraus Yukawa Transition Branch:** (Appendix T, Definition T.41.4a; Theorem T.41.4b; Corollary T.41.4c) Yukawa-magnitude branch on which the Yukawa interaction is represented by the positive PPI-admissible measurement superoperator $\mathcal Y_j(\rho)=P_j\rho P_j$ on rank-one generation projectors. The probability-overlap magnitude $Y_{ij}^{\mathrm{mag}}\propto|\langle\psi_i|\psi_j\rangle|^2=\operatorname{Tr}(P_iP_j)$ is forced by positivity; complex phases are carried separately by the flavor Berry connection (Corollary T.41.4c; Theorems T.53–T.54 and T.24.19–T.24.22). The numerical CKM response phase $66.7°$ additionally requires Theorem T.56's independent ansatz.

*   **$u^*$ (Bare Coupling):** (Appendix Z, Theorem Z.7) The information-theoretic interaction strength at the PCE-Attractor, determined by capacity saturation: $u^*=d_0^{1/M}-1=8^{1/24}-1=2^{1/8}-1\approx 0.09051$. Related to the fine-structure constant through the Predictive Ward Identity (Theorem Z.14, with Thomson-Ward spectral-weight closure via Definition Z.14d and Theorem Z.14e) and Theorems Z.24-Z.26, yielding the closed-form Thomson core $\alpha^{-1}_{0}=137.03609205522863\ldots$ and the candidate diagnostic-only certificate row $\alpha^{-1}_{\mathrm{cand}}=\alpha^{-1}_{0}+R_{\alpha}^{YR\perp}=137.03599917753023\ldots$ (Definition Z.27.11k.12; Theorem Z.27.11k.20; Corollary Z.27.11k.21.1).

*   **UCT (Unified Cost of Transgression):** (Appendix N) A frame-consistent additive work ledger that, on the declared detector-response, additive-temperature, active-refresh, disjoint-ledger, and export branch, combines a predictive-refresh increment with a distinct relativistic endpoint kinetic-work term. It does not identify the SPAP and causal-frontier limits or prove a common microscopic origin.

*   **Unique Spectral Calibration Principle (USCP):** (Appendix V.3.11) Final calibration rule for continuous coefficients. Given a sealed branch and an accepted finite spectral calibration datum $\mathfrak S_*=(\mathcal A_*,\Omega_*,\mathcal Q_{\mathrm{null}},\nu_*,\nu_*^{\mathrm{fs}},\{m_a,c_a^*\},\mathcal Q_{\mathrm{ind}},\mathcal I_*,\{\widehat O_B,\sigma_B,s_B\},\mathcal R^*_{\mu\leftarrow\mu_*},\mathcal C_{\circlearrowleft},\mathcal O_*,\chi_*)$, the calibrated state is the unique minimizer
$$
\omega_*=
\operatorname*{argmin}_{\omega\in\mathcal C_*}
D_{\mathrm{KL}}(\omega\Vert\nu_*),
$$
and every response-active coefficient is the branch-scaled spectral moment $s_B\langle\sigma_B\rangle_{\omega_*}$. USCP does not alter the discrete backbone; it promotes a continuous coefficient to theorem-level only when the calibration algebra, spectral atoms, invariant reference measure, full-support witness, constraints, symbols, unit bridge, RG/threshold route, circular-angle convention, and parent-sector overlap map are fixed before comparison.

*   **$\Upsilon_G$ (Background Coupling Ratio):** (Postulate K.9.2, Theorem K.9.3) The time-dependent non-vacuum gravitational coupling ratio
$$
\Upsilon_G(t):=\frac{G_{eff}(t)}{G_0}=1+A_c\left[1-e^{-s_{bg}(t)^n}\right].
$$

*   **Vacuum–Electroweak Complexity Product Lock:** (Appendix T, Corollary T.62c; Appendix U, Corollaries U.72c–U.72d) On the intersection of the Appendix U five-mode reference counting branch, the independent vacuum counting-to-action and vacuum-weight-to-$\Lambda$ bridges, the Appendix T electroweak counting-to-action and action-to-observable bridges, the common Planck normalization, and the two determinant branches,
$$
(\Lambda L_P^2)(v/M_{Pl})^2
=
8\pi A_{\mathrm{eff}}A_{EW}^2e^{-360}.
$$
On the Theorem U.13b four-mode branch the exponent ledger is $361$. Corollary U.72d shows that forming both corrected observational factors from the same $\Lambda$ cancels the cosmological anchor and leaves exactly $(v_{\mathrm{pred}}/v_{\mathrm{obs}})^2$; this is a cosmological-anchor-free electroweak diagnostic, not an input-free prediction, an independent vacuum-sector test, or a forward vacuum-prefactor calculation.

*   **Vacuum Weight Interpretation of $\Lambda$:** (Remark U.15c.1) Theorem U.13b fixes the four-mode Gaussian counting index $\kappa=142$ under its spectral hypotheses, while $\kappa_{\Lambda,\mathrm{ref}}=141.5$ is a five-mode reference counting convention. The physical relation $\Lambda L_P^2=8\pi A_{\mathrm{eff}}e^{-2\kappa}$ additionally requires the independent equal-contribution action bridge, the vacuum-weight-to-$\Lambda$ map, and a same-branch prefactor and residual certificate.

*   **Validation Target:** A measured or externally inferred value used only to test a fixed branch after the operator, normalization, finite-record, and status ledgers have been fixed. It is not an input to a theorem-level derivation and cannot promote a row by agreement alone.

*   **Ward-Cohomology Residual Gate:** (Appendix Z, Definition Z.27.11k; Theorem Z.27.11k.1; Corollary Z.27.11k.2) Certificate machinery treating $R_\alpha$ as the evaluation of a finite electromagnetic Ward cohomology class. Ward-exact residuals are PPI/PCE-null and cannot shift $\alpha^{-1}$; if $H^1_W(\alpha)=0$, then $R_\alpha=0$. On the candidate hypercharge-recoil branch, the stipulated finite complex has basis $[\omega_Y]$, $[\omega_5]$, and $[\sigma_{\perp}]$, giving the candidate value $R_{\alpha}^{YR\perp}=-0.00009287769839723537\ldots$. The operator realization and source-exhaustion certificate remain open, so the stipulated finite complex does not prove that no additional post-core source exists.

*   **WAY-PCE Measurement Bound:** (Appendix M.6.13) Conservation-law measurement constraint: a charge-covariant finite branch cannot exactly measure a charge-asymmetric observable without reference asymmetry or finite measurement error. The Blackwell-PCE record can report only what this constrained channel can acquire.

*   **Weber-Fechner Relation (PU form):** (Remark 19.3, Equation 5a) The representative logarithmic reflexive-information cost gives $\Delta R_I=(r_I/\ln2)\ln(C_2/C_1)$, so a fixed internal increment $\Delta R_I=\delta_R$ is equivalent to a fixed external ratio $C_2/C_1=\exp(\delta_R\ln2/r_I)$.

*   **Wilson Loop:** (Appendix Z.8d) Gauge-invariant observable $W(C)$ associated with a closed loop $C$. In the flux-tube confinement branch, rootlessness of the Leech lattice motivates an area law $\langle W(C)\rangle\sim e^{-\sigma\mathrm{Area}(C)}$.

*   **Zipf-Mandelbrot Law (PU conditional form):** (Theorem P.8.9a.12.1) Gibbs weights with logarithmic ranked PCE cost $E_r=E_0+\gamma\log(r+r_0)$ give $p_r\propto(r+r_0)^{-\beta\gamma}$.
